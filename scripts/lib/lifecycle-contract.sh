#!/bin/bash
# Generated by scripts/generate-lifecycle-contract.py. Do not edit.
# This file deliberately uses only syntax available in macOS Bash 3.2.

if [ "${GENDEV_LIFECYCLE_CONTRACT_LOADED+x}" = x ]; then
  if [ "$GENDEV_LIFECYCLE_CONTRACT_LOADED" = '252487aed05f12f2c4c296938f8a6d797ac2a0c32ddee16e7c54a8e797826370' ]; then
    if [ "${GENDEV_LIFECYCLE_REGISTRY_SHA256:-}" = '252487aed05f12f2c4c296938f8a6d797ac2a0c32ddee16e7c54a8e797826370' ] && command -v gendev_gate_name >/dev/null 2>&1; then
      return 0 2>/dev/null || exit 0
    fi
    printf '%s\n' 'lifecycle contract: incomplete loaded contract' >&2
    return 2 2>/dev/null || exit 2
  fi
  printf '%s\n' 'lifecycle contract: conflicting loaded contract' >&2
  return 2 2>/dev/null || exit 2
fi

readonly GENDEV_LIFECYCLE_REGISTRY_ID='gendev-lifecycle' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_LIFECYCLE_REGISTRY_ID' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_LIFECYCLE_SCHEMA_VERSION='2' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_LIFECYCLE_SCHEMA_VERSION' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_LIFECYCLE_REGISTRY_STATUS='candidate' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_LIFECYCLE_REGISTRY_STATUS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_LIFECYCLE_TARGET_VERSION='0.5.0-operational-coherence' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_LIFECYCLE_TARGET_VERSION' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_LIFECYCLE_REGISTRY_SHA256='252487aed05f12f2c4c296938f8a6d797ac2a0c32ddee16e7c54a8e797826370' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_LIFECYCLE_REGISTRY_SHA256' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_GATE_IDS='G0 G1 G2 G3 G4 G5 G6 G7 G8 G9' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_GATE_IDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_TERMINAL_GATE='G9' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_TERMINAL_GATE' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_CHECKPOINT_TEMPLATES='G5.0 G5.<id>.1 G5.<id>.2 G5.<id>.3 G5.<id>.4' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_CHECKPOINT_TEMPLATES' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_EVENT_TYPES='project_initialization gate_transition phase_checkpoint phase_transition deployment_approval amendment gate_regression reconciliation migration_reconciliation traceability_sample enforcement_attestation enforcement_override' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_EVENT_TYPES' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_ARTIFACT_IDS='project_manifest gate_log vision prd architecture technology_stack governance_security phase_plan phase_build_plan tactical_plan construction_directive build_prompt phase_test_uat implementation_evidence phase_code_review phase_remediation phase_learnings phase_as_built phase_value_review traceability implementation_summary final_code_review aggregate_remediation final_test_uat deployment_readiness production_runbook deployment_record project_value_review project_as_built supporting_design' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_ARTIFACT_IDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_ARTIFACT_STATUSES='Draft|Ready for Review|Ready for Approval|Accepted|Complete|Stale|Superseded' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_ARTIFACT_STATUSES' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_GATE_STATUSES='pending drafting ready_for_review ready_for_approval approved blocked superseded closed' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_GATE_STATUSES' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_PROJECT_STATUSES='initialized active blocked closed' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_PROJECT_STATUSES' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_PHASE_LOOP_STATUSES='not_started active blocked ready_for_g6 closed legacy_unreconciled' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_PHASE_LOOP_STATUSES' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_PHASE_STATUSES='pending planning authorized in_progress ready_for_exit exited blocked superseded' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_PHASE_STATUSES' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_APPROVAL_DECISIONS='approved rejected blocked superseded' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_APPROVAL_DECISIONS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_REMEDIATION_DISPOSITIONS='complete not_required blocked' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_REMEDIATION_DISPOSITIONS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_VALUE_REVIEW_DISPOSITIONS='complete not_due not_applicable' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_VALUE_REVIEW_DISPOSITIONS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_VALUE_RESULTS='met missed unmeasurable' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_VALUE_RESULTS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_ENFORCEMENT_CLASSES='attested guarded protected' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_ENFORCEMENT_CLASSES' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_VALUE_REVIEW_ARTIFACT_STATUS_IS_SEPARATE_FROM_DISPOSITION='true' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_VALUE_REVIEW_ARTIFACT_STATUS_IS_SEPARATE_FROM_DISPOSITION' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_VALUE_REVIEW_UNOWNED_FUTURE_WORK_IS_INVALID='true' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_VALUE_REVIEW_UNOWNED_FUTURE_WORK_IS_INVALID' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_VALUE_REVIEW_UNMEASURABLE_IS_NOT_SUCCESS='true' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_VALUE_REVIEW_UNMEASURABLE_IS_NOT_SUCCESS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_BLAST_RADIUS_CLASSES='C1 C2 C3' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_BLAST_RADIUS_CLASSES' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_EVIDENCE_CATEGORIES='new_acceptance_status_only complete_report_unchanged accepted_authority_unchanged' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_EVIDENCE_CATEGORIES' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_CRITERION_IDS='G0-MANIFEST G0-SCAFFOLD G0-IDENTITY G1-PROBLEM G1-USERS G1-SUCCESS G1-NON-GOALS G1-RISKS-QUESTIONS G2-REQ-ID G2-AC-COVERAGE G2-EARS-FORM G2-OBSERVABLE-FORM G2-UNWANTED-BEHAVIOR G2-TESTABILITY G3-ARCH-TRACE G3-BOUNDARIES G3-STATE-LIFECYCLE G3-STACK-ACCEPTED G3-VERIFICATION-SPEC G3-VERIFICATION-TRACE G3-DESIGN-INTERROGATION G4-ACTOR-MODEL G4-AUTHORIZATION G4-AUDIT G4-SECRETS-DATA G4-TRUST-TOOLS G4-NEGATIVE-TESTS G5-PHASE-ORDER G5-REQ-COVERAGE G5-INTEGRATION G5-PARTITION G5-COVERAGE-POLICY G5-PHASE-SCOPE G5-PHASE-EXIT-TEST-DESIGN G5-TACTICAL-TASKS G5-TACTICAL-DEPENDENCIES G5-TACTICAL-PATHS G5-TACTICAL-TESTS G5-TACTICAL-VERIFICATION G5-TACTICAL-ROLLBACK G5-DIRECTIVE-COMPLETE G5-BUILD-PROMPT G5-TEST-PLAN-ACCEPTED G5-UPSTREAM-PINNED G5-IMPLEMENTATION-APPROVAL G5-EXIT-IMPLEMENTATION G5-EXIT-TEST-UAT G5-EXIT-REVIEW G5-EXIT-REMEDIATION G5-EXIT-TRACEABILITY G5-EXIT-AS-BUILT G5-EXIT-LEARNINGS G5-EXIT-REGRESSION G5-EXIT-COVERAGE G5-EXIT-RESIDUALS G5-EXIT-AMENDMENTS G5-EXIT-VALUE-DISPOSITION G5-EXIT-APPROVAL G5-LOOP-AUTHORIZED G5-ALL-PHASES-EXITED G5-WHOLE-CANDIDATE G5-INTEGRATION-REGRESSION G5-CURRENT-TRACEABILITY G5-RESIDUAL-DISPOSITION G6-IMPLEMENTATION-SUMMARY G6-CANDIDATE-REVISION G6-CHANGED-FILES G6-INTEGRATION-REGRESSION G6-DEVIATIONS-RESIDUALS G6-REVIEW-PACKAGE G7-INDEPENDENT-REVIEW G7-REMEDIATION G7-FINAL-UAT G7-TRACEABILITY G7-RESIDUAL-RISK G7-UPSTREAM-BINDINGS G8-READINESS G8-RUNBOOK G8-RELEASE-SCOPE G8-CONFIG-SECRETS G8-MIGRATION-ROLLBACK G8-MONITORING-OWNERSHIP G8-VALUE-PREREQUISITES G8-DEPLOYMENT-APPROVAL G9-DEPLOYMENT-DISPOSITION G9-OPERATIONS-RESULTS G9-VALUE-DISPOSITION G9-FINAL-TRACEABILITY G9-AS-BUILT-CLOSEOUT' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_CRITERION_IDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_ROLE_IDS='none product-vision-agent prd-agent architecture-agent security-governance-agent phase-planning-agent implementation-agent test-uat-agent code-review-agent remediation-agent deployment-readiness-agent as-built-closeout-agent named-human-approver named-human-deployment-approver named-human-security-approver named-human-operational-owner' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_ROLE_IDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_CANONICAL_DIRECTORIES='docs/project/approvals docs/project/architecture docs/project/as-built docs/project/build-plan docs/project/build-plan/phases docs/project/decisions docs/project/deployment docs/project/design docs/project/prd docs/project/review docs/project/security-governance docs/project/testing docs/project/traceability docs/project/vision' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_CANONICAL_DIRECTORIES' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_FORBIDDEN_DIRECTORIES='docs/project/supporting' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_FORBIDDEN_DIRECTORIES' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_REFERENCE_RELATIONSHIPS='implements satisfies tested-by constrained-by refines' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_REFERENCE_RELATIONSHIPS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_DEPLOYMENT_INTENTS='deploy non_deployment' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_DEPLOYMENT_INTENTS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_APPROVAL_POLICY_IDS='phase_checkpoint_acceptance phase_exit implementation_acceptance deployment project_closeout' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_APPROVAL_POLICY_IDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_EVENT_BINDING_RULE_IDS='all_declared_phase_exit_event_ids accepted_upstream_authority_set deployment_approval_or_non_deployment_approval' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_EVENT_BINDING_RULE_IDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_EVENT_CORRECTION_FIELDS='supersedes_event_id correction_reason' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_EVENT_CORRECTION_FIELDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_EVENT_HISTORY_ENFORCEMENT_BEHAVIORS='append_only correction_event duplicate_latest_rejected supersession_cycle_rejected' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_EVENT_HISTORY_ENFORCEMENT_BEHAVIORS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_EVENT_REFERENCE_FIELDS='artifact_id artifact_path status revision blob_oid digest' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_EVENT_REFERENCE_FIELDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_EVENT_REFERENCE_DIGEST_ALGORITHM='sha256' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_EVENT_REFERENCE_DIGEST_ALGORITHM' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_EVENT_REFERENCE_CANNOT_SATISFY_ACCEPTANCE='true' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_EVENT_REFERENCE_CANNOT_SATISFY_ACCEPTANCE' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_EVENT_EVIDENCE_FIELDS='artifact_id artifact_path category reviewed_revision reviewed_blob_oid reviewed_digest resulting_blob_oid resulting_digest status' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_EVENT_EVIDENCE_FIELDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_EVENT_EVIDENCE_CONDITIONS='accepted_authority_unchanged' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_EVENT_EVIDENCE_CONDITIONS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_EVENT_SERIALIZATION_PROFILE='restricted_yaml_v2' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_EVENT_SERIALIZATION_PROFILE' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_EVENT_SERIALIZATION_SCHEMA_VERSION='2' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_EVENT_SERIALIZATION_SCHEMA_VERSION' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_EVENT_FIELD_IDS='amendment_id approval_date approval_disposition approval_profile approved_by approved_on approver attested_by attested_on authority_path blocking_finding_count candidate_revision change_scope checked_statement correction_reason decision deployment_disposition deployment_intent discrepancy discrepancy_disposition disposition event_id event_type from_gate future_trigger_or_finality gate gate_movement_unblocked impact incident_or_emergency major_gate mapped_gate_or_checkpoint no_additional_approval_basis normal_enforcement_resumed_on occurred_on phase_id position project rationale reason reconciled_by reconciled_on reconciliation_required recorded_by recorded_on release_candidate remediation_disposition requirement_id result sampled_by sampled_on schema_version scope source_methodology_version supersedes_event_id target_methodology_version terminal_closeout to_gate traceability_row amendments criterion_ids invalidated_criteria mapped_evidence_classes phase_requirement_ids remaining_stale_artifacts requirements_bypassed requirements_checked unresolved_fields coverage_result delegation enforcement_context historical_event_reference manifest_result next_state operational_owner_confirmation operational_results regression_result risk_disposition security_approval test_uat_execution value_disposition value_prerequisites artifacts evidence exceptions provenance references residual_findings' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_EVENT_FIELD_IDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_EVENT_RECORD_CONTRACT_IDS='event_evidence_item event_reference_item coverage_result delegation enforcement_context historical_event_reference manifest_result next_state operational_owner_confirmation operational_results regression_result risk_disposition security_approval test_uat_execution command_result_item value_review value_review_details value_result_item reconciliation_artifact_item residual_finding_item migration_provenance_item enforcement_exception_item' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_EVENT_RECORD_CONTRACT_IDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_EVENT_COMMON_CONDITIONAL_PROFILES='correction' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_EVENT_COMMON_CONDITIONAL_PROFILES' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_EVENT_DEFAULT_SCALAR_VALUE_CONTRACT='nonempty_string' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_EVENT_DEFAULT_SCALAR_VALUE_CONTRACT' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_DEPLOYMENT_REQUIRED_ARTIFACTS='deployment_readiness production_runbook' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_DEPLOYMENT_REQUIRED_ARTIFACTS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_DEPLOYMENT_CRITERION_IDS='G8-READINESS G8-RUNBOOK G8-RELEASE-SCOPE G8-CONFIG-SECRETS G8-MIGRATION-ROLLBACK G8-MONITORING-OWNERSHIP G8-VALUE-PREREQUISITES G8-DEPLOYMENT-APPROVAL' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_DEPLOYMENT_CRITERION_IDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_DEPLOYMENT_VALUE_PREREQUISITE_FIELDS='disposition details' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_DEPLOYMENT_VALUE_PREREQUISITE_FIELDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_DEPLOYMENT_VALUE_CONTRACT='value_review_contract' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_DEPLOYMENT_VALUE_CONTRACT' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_DEPLOYMENT_AUTHORIZATION_EVENT='deployment_approval' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_DEPLOYMENT_AUTHORIZATION_EVENT' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_DEPLOYMENT_TERMINAL_TRANSITION='G8-to-G9' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_DEPLOYMENT_TERMINAL_TRANSITION' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_DEPLOYMENT_NONDEPLOYMENT_REQUIRED_FIELDS='disposition rationale scope release_candidate approver approved_on future_trigger_or_finality' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_DEPLOYMENT_NONDEPLOYMENT_REQUIRED_FIELDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_COMBINED_GATE_REQUIRED_FIELDS='span blast_radius_class mode justification preserved_criterion_ids evidence approved_by approved_on event_id' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_COMBINED_GATE_REQUIRED_FIELDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_COVERAGE_REQUIRED_FIELDS='metric measurement_tool denominator target scope measurement_command exclusions exclusion_rationale owner shortfall_disposition' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_COVERAGE_REQUIRED_FIELDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_MANIFEST_REQUIRED_FIELDS='project.name project.slug project.status project.current_gate project.methodology_version human_control.owner human_control.approver human_control.deployment_approver collaboration.active_role scaling.blast_radius_class approvals.current_gate approvals.latest_decision phase.loop_status phase.phase_position phase.current_phase_id phase.phase_plan phase.phases' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_MANIFEST_REQUIRED_FIELDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_MANIFEST_INVARIANT_IDS='MANIFEST-GATE-MAJOR MANIFEST-PHASE-AXIS MANIFEST-PHASE-MEMBER MANIFEST-PHASE-ORDER MANIFEST-LATEST-SUMMARY MANIFEST-TERMINAL MANIFEST-STRICT-V2' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_MANIFEST_INVARIANT_IDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_MANIFEST_SCHEMA_VERSION='2' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_MANIFEST_SCHEMA_VERSION' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_MANIFEST_SOURCE_FILE='docs/project-template/project.yaml' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_MANIFEST_SOURCE_FILE' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_MANIFEST_CONTRACT_STATE='planned' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_MANIFEST_CONTRACT_STATE' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_MANIFEST_REQUIRED_WORK_PACKAGE='WP-04' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_MANIFEST_REQUIRED_WORK_PACKAGE' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_REFERENCE_DEPTH_EXCEPTION_FIELDS='declared_by justification maximum_depth approved_by approved_on' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_REFERENCE_DEPTH_EXCEPTION_FIELDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_REFERENCE_RULE_IDS='REF-TARGET-KIND REF-CANONICAL-PATH REF-SUPPORTING-PATH REF-AUTHORITY-DIRECTION REF-NO-CYCLE REF-DEPTH' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_REFERENCE_RULE_IDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_REFERENCE_ENFORCEMENT_REQUIRED_RULE_IDS='REF-TARGET-KIND REF-CANONICAL-PATH REF-SUPPORTING-PATH REF-AUTHORITY-DIRECTION REF-NO-CYCLE REF-DEPTH' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_REFERENCE_ENFORCEMENT_REQUIRED_RULE_IDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_RATIFICATION_REVIEWED_STATUS='Ready for Approval' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_RATIFICATION_REVIEWED_STATUS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_RATIFICATION_RESULTING_STATUS='Accepted' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_RATIFICATION_RESULTING_STATUS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_RATIFICATION_EVIDENCE_CATEGORY='new_acceptance_status_only' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_RATIFICATION_EVIDENCE_CATEGORY' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_REFERENCE_DEFAULT_SUPPORTING_DEPTH='1' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_REFERENCE_DEFAULT_SUPPORTING_DEPTH' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_PHASE_ID_PATTERN='^[A-Za-z0-9]+(-[A-Za-z0-9]+)*$' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_PHASE_ID_PATTERN' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_WORKSTREAM_ID_PATTERN='^PH-([A-Za-z0-9]+(-[A-Za-z0-9]+)*)-WS([0-9]{2})$' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_WORKSTREAM_ID_PATTERN' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_TASK_ID_PATTERN='^PH-([A-Za-z0-9]+(-[A-Za-z0-9]+)*)-T([0-9]{3})$' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_TASK_ID_PATTERN' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_CHECKPOINT_PATTERN='^G5(\.0|\.[A-Za-z0-9]+(-[A-Za-z0-9]+)*\.[1-4])$' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_CHECKPOINT_PATTERN' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_EVENT_ID_PATTERN='^EV-[0-9]{8}-[A-Za-z0-9][A-Za-z0-9-]*$' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_EVENT_ID_PATTERN' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_TASK_IMMUTABLE_AFTER_STATUS='Accepted' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_TASK_IMMUTABLE_AFTER_STATUS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_TASK_REUSE_RETIRED_IDS='false' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_TASK_REUSE_RETIRED_IDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_WORKSTREAM_IMMUTABLE_AFTER_STATUS='Accepted' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_WORKSTREAM_IMMUTABLE_AFTER_STATUS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_WORKSTREAM_REUSE_RETIRED_IDS='false' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_WORKSTREAM_REUSE_RETIRED_IDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_COMPATIBILITY_NEW_PROJECT_MODE='strict_schema_v2' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_COMPATIBILITY_NEW_PROJECT_MODE' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_COMPATIBILITY_LEGACY_MODE='explicit_version_bound_migration' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_COMPATIBILITY_LEGACY_MODE' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_COMPATIBILITY_LEGACY_EVENT_POLICY='Historical unknowns are recorded, never fabricated.' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_COMPATIBILITY_LEGACY_EVENT_POLICY' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_COMPATIBILITY_NEW_EVENTS_IN_LEGACY_MODE='must_conform_to_schema_v2' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_COMPATIBILITY_NEW_EVENTS_IN_LEGACY_MODE' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_COMPATIBILITY_AUTOMATIC_GATE_REGRESSION='false' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_COMPATIBILITY_AUTOMATIC_GATE_REGRESSION' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_COMPATIBILITY_SCAFFOLD_FRESH_INIT='project_wide_only' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_COMPATIBILITY_SCAFFOLD_FRESH_INIT' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_COMPATIBILITY_SCAFFOLD_PHASE_COMMAND='scripts/init-phase.sh' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_COMPATIBILITY_SCAFFOLD_PHASE_COMMAND' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_COMPATIBILITY_SCAFFOLD_STATE='planned' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_COMPATIBILITY_SCAFFOLD_STATE' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_COMPATIBILITY_SCAFFOLD_REQUIRED_WORK_PACKAGE='WP-04' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_COMPATIBILITY_SCAFFOLD_REQUIRED_WORK_PACKAGE' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_COMPATIBILITY_SCAFFOLD_SEED_PHASE_OPTION='--seed-phase' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_COMPATIBILITY_SCAFFOLD_SEED_PHASE_OPTION' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_COMPATIBILITY_SCAFFOLD_SEED_PHASE_MUST_BE_COMPLETE='true' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_COMPATIBILITY_SCAFFOLD_SEED_PHASE_MUST_BE_COMPLETE' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_MIGRATION_ALLOWED_DECISIONS='mapped partially_mapped not_mapped' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_MIGRATION_ALLOWED_DECISIONS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_MIGRATION_REFERENCE_KINDS='stable_event_id content_digest' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_MIGRATION_REFERENCE_KINDS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_MIGRATION_REFERENCE_DIGEST_ALGORITHM='sha256' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_MIGRATION_REFERENCE_DIGEST_ALGORITHM' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_MIGRATION_LINE_NUMBER_ONLY_FORBIDDEN='true' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_MIGRATION_LINE_NUMBER_ONLY_FORBIDDEN' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_MIGRATION_NAMED_HUMAN_REQUIRED_WHEN='blast_radius_C3 unresolved_approval_provenance unresolved_security_provenance residual_historical_uncertainty_accepted' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_MIGRATION_NAMED_HUMAN_REQUIRED_WHEN' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_MIGRATION_CRITICAL_UNCERTAINTY_WAIVABLE='false' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_MIGRATION_CRITICAL_UNCERTAINTY_WAIVABLE' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_MIGRATION_AUTOMATION_MAY_APPROVE='false' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_MIGRATION_AUTOMATION_MAY_APPROVE' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_MIGRATION_UNRESOLVED_FIELDS_PROPAGATE_TO_READINESS='true' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_MIGRATION_UNRESOLVED_FIELDS_PROPAGATE_TO_READINESS' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_MIGRATION_DUPLICATE_MAPPING_REQUIRES_SUPERSEDES='true' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_MIGRATION_DUPLICATE_MAPPING_REQUIRES_SUPERSEDES' >&2
  return 2 2>/dev/null || exit 2
}

gendev_list_contains() {
  [ "$#" -eq 2 ] || return 2
  case " $1 " in
    *" $2 "*) return 0 ;;
    *) return 1 ;;
  esac
}

gendev_approval_string() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'phase_checkpoint_acceptance:approver_kind') printf '%s\n' 'named_human' ;;
    'phase_exit:approver_kind') printf '%s\n' 'named_human' ;;
    'phase_exit:delegation_event_type') printf '%s\n' 'phase_transition' ;;
    'phase_exit:delegation_event_profile') printf '%s\n' 'delegated_phase_exit' ;;
    'phase_exit:delegation_event_field') printf '%s\n' 'delegation' ;;
    'implementation_acceptance:approver_kind') printf '%s\n' 'named_human' ;;
    'deployment:approver_kind') printf '%s\n' 'named_human_deployment_approver' ;;
    'project_closeout:approver_kind') printf '%s\n' 'named_human' ;;
    *) return 1 ;;
  esac
}

gendev_approval_boolean() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'phase_checkpoint_acceptance:changes_major_gate') printf '%s\n' 'false' ;;
    'phase_checkpoint_acceptance:automation_or_unbound_role_may_approve') printf '%s\n' 'false' ;;
    'phase_exit:c1_c2_delegation_allowed') printf '%s\n' 'true' ;;
    'phase_exit:c3_delegation_allowed') printf '%s\n' 'false' ;;
    'phase_exit:automation_or_unbound_role_may_approve') printf '%s\n' 'false' ;;
    'phase_exit:critical_findings_block') printf '%s\n' 'true' ;;
    'phase_exit:major_residual_requires_named_human_risk_acceptance') printf '%s\n' 'true' ;;
    'implementation_acceptance:automation_or_unbound_role_may_approve') printf '%s\n' 'false' ;;
    'deployment:security_approver_when_governance_requires') printf '%s\n' 'true' ;;
    'deployment:automation_or_unbound_role_may_approve') printf '%s\n' 'false' ;;
    'project_closeout:operational_owner_confirmation_when_deployed') printf '%s\n' 'true' ;;
    'project_closeout:automation_or_unbound_role_may_approve') printf '%s\n' 'false' ;;
    *) return 1 ;;
  esac
}

gendev_approval_fields() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'phase_exit:delegation_allowed_classes') printf '%s\n' 'C1 C2' ;;
    'phase_exit:delegation_prohibited_classes') printf '%s\n' 'C3' ;;
    'phase_exit:c1_c2_delegation_required_fields') printf '%s\n' 'delegated_by delegated_to accepted_by scope starts_on ends_on' ;;
    *) return 1 ;;
  esac
}

gendev_approval_approver_kind() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'phase_checkpoint_acceptance') printf '%s\n' 'named_human' ;;
    'phase_exit') printf '%s\n' 'named_human' ;;
    'implementation_acceptance') printf '%s\n' 'named_human' ;;
    'deployment') printf '%s\n' 'named_human_deployment_approver' ;;
    'project_closeout') printf '%s\n' 'named_human' ;;
    *) return 1 ;;
  esac
}

gendev_combined_gate_rule() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'c3_prohibited') printf '%s\n' 'true' ;;
    'nonadjacent_requires_declared_decision') printf '%s\n' 'true' ;;
    'all_spanned_criteria_preserved') printf '%s\n' 'true' ;;
    'nonwaivable_evidence_preserved') printf '%s\n' 'true' ;;
    'phase_exit_may_combine_form_not_evidence') printf '%s\n' 'true' ;;
    *) return 1 ;;
  esac
}

readonly GENDEV_COVERAGE_UNIVERSAL_PERCENTAGE='' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_COVERAGE_UNIVERSAL_PERCENTAGE' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_COVERAGE_SHORTFALL_REQUIRES_NAMED_RISK_ACCEPTANCE='true' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_COVERAGE_SHORTFALL_REQUIRES_NAMED_RISK_ACCEPTANCE' >&2
  return 2 2>/dev/null || exit 2
}

gendev_event_evidence_conditional_fields() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'accepted_authority_unchanged') printf '%s\n' 'originating_event_id' ;;
    *) return 1 ;;
  esac
}

gendev_event_evidence_revision_string() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'portable_digest_algorithm') printf '%s\n' 'sha256' ;;
    *) return 1 ;;
  esac
}

gendev_event_evidence_revision_boolean() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'must_resolve_in_git') printf '%s\n' 'true' ;;
    'dirty_untracked_missing_or_placeholder_rejected') printf '%s\n' 'true' ;;
    'reviewed_blob_must_match_revision_path') printf '%s\n' 'true' ;;
    'one_category_exactly') printf '%s\n' 'true' ;;
    *) return 1 ;;
  esac
}

gendev_event_field_shape() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'amendment_id') printf '%s\n' 'scalar' ;;
    'approval_date') printf '%s\n' 'scalar' ;;
    'approval_disposition') printf '%s\n' 'scalar' ;;
    'approval_profile') printf '%s\n' 'scalar' ;;
    'approved_by') printf '%s\n' 'scalar' ;;
    'approved_on') printf '%s\n' 'scalar' ;;
    'approver') printf '%s\n' 'scalar' ;;
    'attested_by') printf '%s\n' 'scalar' ;;
    'attested_on') printf '%s\n' 'scalar' ;;
    'authority_path') printf '%s\n' 'scalar' ;;
    'blocking_finding_count') printf '%s\n' 'scalar' ;;
    'candidate_revision') printf '%s\n' 'scalar' ;;
    'change_scope') printf '%s\n' 'scalar' ;;
    'checked_statement') printf '%s\n' 'scalar' ;;
    'correction_reason') printf '%s\n' 'scalar' ;;
    'decision') printf '%s\n' 'scalar' ;;
    'deployment_disposition') printf '%s\n' 'scalar' ;;
    'deployment_intent') printf '%s\n' 'scalar' ;;
    'discrepancy') printf '%s\n' 'scalar' ;;
    'discrepancy_disposition') printf '%s\n' 'scalar' ;;
    'disposition') printf '%s\n' 'scalar' ;;
    'event_id') printf '%s\n' 'scalar' ;;
    'event_type') printf '%s\n' 'scalar' ;;
    'from_gate') printf '%s\n' 'scalar' ;;
    'future_trigger_or_finality') printf '%s\n' 'scalar' ;;
    'gate') printf '%s\n' 'scalar' ;;
    'gate_movement_unblocked') printf '%s\n' 'scalar' ;;
    'impact') printf '%s\n' 'scalar' ;;
    'incident_or_emergency') printf '%s\n' 'scalar' ;;
    'major_gate') printf '%s\n' 'scalar' ;;
    'mapped_gate_or_checkpoint') printf '%s\n' 'scalar' ;;
    'no_additional_approval_basis') printf '%s\n' 'scalar' ;;
    'normal_enforcement_resumed_on') printf '%s\n' 'scalar' ;;
    'occurred_on') printf '%s\n' 'scalar' ;;
    'phase_id') printf '%s\n' 'scalar' ;;
    'position') printf '%s\n' 'scalar' ;;
    'project') printf '%s\n' 'scalar' ;;
    'rationale') printf '%s\n' 'scalar' ;;
    'reason') printf '%s\n' 'scalar' ;;
    'reconciled_by') printf '%s\n' 'scalar' ;;
    'reconciled_on') printf '%s\n' 'scalar' ;;
    'reconciliation_required') printf '%s\n' 'scalar' ;;
    'recorded_by') printf '%s\n' 'scalar' ;;
    'recorded_on') printf '%s\n' 'scalar' ;;
    'release_candidate') printf '%s\n' 'scalar' ;;
    'remediation_disposition') printf '%s\n' 'scalar' ;;
    'requirement_id') printf '%s\n' 'scalar' ;;
    'result') printf '%s\n' 'scalar' ;;
    'sampled_by') printf '%s\n' 'scalar' ;;
    'sampled_on') printf '%s\n' 'scalar' ;;
    'schema_version') printf '%s\n' 'scalar' ;;
    'scope') printf '%s\n' 'scalar' ;;
    'source_methodology_version') printf '%s\n' 'scalar' ;;
    'supersedes_event_id') printf '%s\n' 'scalar' ;;
    'target_methodology_version') printf '%s\n' 'scalar' ;;
    'terminal_closeout') printf '%s\n' 'scalar' ;;
    'to_gate') printf '%s\n' 'scalar' ;;
    'traceability_row') printf '%s\n' 'scalar' ;;
    'amendments') printf '%s\n' 'scalar_list' ;;
    'criterion_ids') printf '%s\n' 'scalar_list' ;;
    'invalidated_criteria') printf '%s\n' 'scalar_list' ;;
    'mapped_evidence_classes') printf '%s\n' 'scalar_list' ;;
    'phase_requirement_ids') printf '%s\n' 'scalar_list' ;;
    'remaining_stale_artifacts') printf '%s\n' 'scalar_list' ;;
    'requirements_bypassed') printf '%s\n' 'scalar_list' ;;
    'requirements_checked') printf '%s\n' 'scalar_list' ;;
    'unresolved_fields') printf '%s\n' 'scalar_list' ;;
    'coverage_result') printf '%s\n' 'record' ;;
    'delegation') printf '%s\n' 'record' ;;
    'enforcement_context') printf '%s\n' 'record' ;;
    'historical_event_reference') printf '%s\n' 'record' ;;
    'manifest_result') printf '%s\n' 'record' ;;
    'next_state') printf '%s\n' 'record' ;;
    'operational_owner_confirmation') printf '%s\n' 'record' ;;
    'operational_results') printf '%s\n' 'record' ;;
    'regression_result') printf '%s\n' 'record' ;;
    'risk_disposition') printf '%s\n' 'record' ;;
    'security_approval') printf '%s\n' 'record' ;;
    'test_uat_execution') printf '%s\n' 'record' ;;
    'value_disposition') printf '%s\n' 'record' ;;
    'value_prerequisites') printf '%s\n' 'record' ;;
    'artifacts') printf '%s\n' 'record_list' ;;
    'evidence') printf '%s\n' 'record_list' ;;
    'exceptions') printf '%s\n' 'record_list' ;;
    'provenance') printf '%s\n' 'record_list' ;;
    'references') printf '%s\n' 'record_list' ;;
    'residual_findings') printf '%s\n' 'record_list' ;;
    *) return 1 ;;
  esac
}

gendev_event_field_item_contract() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'coverage_result') printf '%s\n' 'coverage_result' ;;
    'delegation') printf '%s\n' 'delegation' ;;
    'enforcement_context') printf '%s\n' 'enforcement_context' ;;
    'historical_event_reference') printf '%s\n' 'historical_event_reference' ;;
    'manifest_result') printf '%s\n' 'manifest_result' ;;
    'next_state') printf '%s\n' 'next_state' ;;
    'operational_owner_confirmation') printf '%s\n' 'operational_owner_confirmation' ;;
    'operational_results') printf '%s\n' 'operational_results' ;;
    'regression_result') printf '%s\n' 'regression_result' ;;
    'risk_disposition') printf '%s\n' 'risk_disposition' ;;
    'security_approval') printf '%s\n' 'security_approval' ;;
    'test_uat_execution') printf '%s\n' 'test_uat_execution' ;;
    'value_disposition') printf '%s\n' 'value_review' ;;
    'value_prerequisites') printf '%s\n' 'value_review' ;;
    'artifacts') printf '%s\n' 'reconciliation_artifact_item' ;;
    'evidence') printf '%s\n' 'event_evidence_item' ;;
    'exceptions') printf '%s\n' 'enforcement_exception_item' ;;
    'provenance') printf '%s\n' 'migration_provenance_item' ;;
    'references') printf '%s\n' 'event_reference_item' ;;
    'residual_findings') printf '%s\n' 'residual_finding_item' ;;
    *) return 1 ;;
  esac
}

gendev_event_field_min_items() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'criterion_ids') printf '%s\n' '1' ;;
    'invalidated_criteria') printf '%s\n' '1' ;;
    'requirements_bypassed') printf '%s\n' '1' ;;
    'requirements_checked') printf '%s\n' '1' ;;
    'artifacts') printf '%s\n' '1' ;;
    'evidence') printf '%s\n' '1' ;;
    'provenance') printf '%s\n' '1' ;;
    *) return 1 ;;
  esac
}

gendev_event_field_value_contract() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'amendment_id') printf '%s\n' 'nonempty_string' ;;
    'approval_date') printf '%s\n' 'iso_date' ;;
    'approval_disposition') printf '%s\n' 'nonempty_string' ;;
    'approval_profile') printf '%s\n' 'nonempty_string' ;;
    'approved_by') printf '%s\n' 'nonempty_string' ;;
    'approved_on') printf '%s\n' 'iso_date' ;;
    'approver') printf '%s\n' 'nonempty_string' ;;
    'attested_by') printf '%s\n' 'nonempty_string' ;;
    'attested_on') printf '%s\n' 'iso_date' ;;
    'authority_path') printf '%s\n' 'nonempty_string' ;;
    'blocking_finding_count') printf '%s\n' 'nonnegative_integer' ;;
    'candidate_revision') printf '%s\n' 'git_revision' ;;
    'change_scope') printf '%s\n' 'nonempty_string' ;;
    'checked_statement') printf '%s\n' 'nonempty_string' ;;
    'correction_reason') printf '%s\n' 'nonempty_string' ;;
    'decision') printf '%s\n' 'contextual_event_decision' ;;
    'deployment_disposition') printf '%s\n' 'deployment_terminal_disposition' ;;
    'deployment_intent') printf '%s\n' 'deployment_intent' ;;
    'discrepancy') printf '%s\n' 'nonempty_string' ;;
    'discrepancy_disposition') printf '%s\n' 'nonempty_string' ;;
    'disposition') printf '%s\n' 'nonempty_string' ;;
    'event_id') printf '%s\n' 'event_id_pattern' ;;
    'event_type') printf '%s\n' 'event_type_id' ;;
    'from_gate') printf '%s\n' 'gate_id' ;;
    'future_trigger_or_finality') printf '%s\n' 'nonempty_string' ;;
    'gate') printf '%s\n' 'gate_id' ;;
    'gate_movement_unblocked') printf '%s\n' 'boolean' ;;
    'impact') printf '%s\n' 'nonempty_string' ;;
    'incident_or_emergency') printf '%s\n' 'boolean' ;;
    'major_gate') printf '%s\n' 'gate_id' ;;
    'mapped_gate_or_checkpoint') printf '%s\n' 'nonempty_string' ;;
    'no_additional_approval_basis') printf '%s\n' 'nonempty_string' ;;
    'normal_enforcement_resumed_on') printf '%s\n' 'iso_date_or_null' ;;
    'occurred_on') printf '%s\n' 'iso_date' ;;
    'phase_id') printf '%s\n' 'phase_id_or_null' ;;
    'position') printf '%s\n' 'checkpoint_position' ;;
    'project') printf '%s\n' 'nonempty_string' ;;
    'rationale') printf '%s\n' 'nonempty_string' ;;
    'reason') printf '%s\n' 'nonempty_string' ;;
    'reconciled_by') printf '%s\n' 'nonempty_string' ;;
    'reconciled_on') printf '%s\n' 'iso_date' ;;
    'reconciliation_required') printf '%s\n' 'boolean' ;;
    'recorded_by') printf '%s\n' 'nonempty_string' ;;
    'recorded_on') printf '%s\n' 'iso_date' ;;
    'release_candidate') printf '%s\n' 'git_revision' ;;
    'remediation_disposition') printf '%s\n' 'remediation_disposition' ;;
    'requirement_id') printf '%s\n' 'nonempty_string' ;;
    'result') printf '%s\n' 'nonempty_string' ;;
    'sampled_by') printf '%s\n' 'nonempty_string' ;;
    'sampled_on') printf '%s\n' 'iso_date' ;;
    'schema_version') printf '%s\n' 'fixed:2' ;;
    'scope') printf '%s\n' 'nonempty_string' ;;
    'source_methodology_version') printf '%s\n' 'nonempty_string' ;;
    'supersedes_event_id') printf '%s\n' 'event_id_pattern' ;;
    'target_methodology_version') printf '%s\n' 'nonempty_string' ;;
    'terminal_closeout') printf '%s\n' 'boolean' ;;
    'to_gate') printf '%s\n' 'gate_id' ;;
    'traceability_row') printf '%s\n' 'nonempty_string' ;;
    *) return 1 ;;
  esac
}

gendev_event_common_conditional_fields() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'correction') printf '%s\n' 'supersedes_event_id correction_reason' ;;
    *) return 1 ;;
  esac
}

gendev_event_record_required_fields() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'event_evidence_item') printf '%s\n' 'artifact_id artifact_path category reviewed_revision reviewed_blob_oid reviewed_digest resulting_blob_oid resulting_digest status' ;;
    'event_reference_item') printf '%s\n' 'artifact_id artifact_path status revision blob_oid digest' ;;
    'coverage_result') printf '%s\n' 'policy_reference metric measurement_tool denominator target actual result scope measurement_command exclusions exclusion_rationale owner shortfall_disposition' ;;
    'delegation') printf '%s\n' 'delegated_by delegated_to accepted_by scope starts_on ends_on' ;;
    'enforcement_context') printf '%s\n' 'enforcement_class blast_radius_class checker_result exceptions' ;;
    'historical_event_reference') printf '%s\n' 'kind' ;;
    'manifest_result') printf '%s\n' 'project_status current_gate active_role phase_position current_phase_id latest_decision_event_id manifest_digest' ;;
    'next_state') printf '%s\n' 'project_status current_gate active_role phase_position next_artifact_id' ;;
    'operational_owner_confirmation') printf '%s\n' 'confirmed_by confirmed_on checked_statement' ;;
    'operational_results') printf '%s\n' 'production_action_performed rollback_status incident_status monitoring_status evidence_event_ids' ;;
    'regression_result') printf '%s\n' 'candidate_revision commands result phases_covered' ;;
    'risk_disposition') printf '%s\n' 'disposition rationale' ;;
    'security_approval') printf '%s\n' 'required decision' ;;
    'test_uat_execution') printf '%s\n' 'candidate_revision command_results evidence_event_ids' ;;
    'command_result_item') printf '%s\n' 'command result' ;;
    'value_review') printf '%s\n' 'disposition details' ;;
    'value_review_details') printf '%s\n' '' ;;
    'value_result_item') printf '%s\n' 'criterion_id evidence result' ;;
    'reconciliation_artifact_item') printf '%s\n' 'artifact_id artifact_path status action evidence_event_id' ;;
    'residual_finding_item') printf '%s\n' 'finding_id severity disposition owner rationale' ;;
    'migration_provenance_item') printf '%s\n' 'artifact_path reviewed_revision blob_oid digest' ;;
    'enforcement_exception_item') printf '%s\n' 'requirement_id disposition rationale approved_by approved_on' ;;
    *) return 1 ;;
  esac
}

gendev_event_record_field_ids() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'event_evidence_item') printf '%s\n' 'artifact_id artifact_path category reviewed_revision reviewed_blob_oid reviewed_digest resulting_blob_oid resulting_digest status originating_event_id' ;;
    'event_reference_item') printf '%s\n' 'artifact_id artifact_path status revision blob_oid digest' ;;
    'coverage_result') printf '%s\n' 'policy_reference metric measurement_tool denominator target actual result scope measurement_command exclusions exclusion_rationale owner shortfall_disposition' ;;
    'delegation') printf '%s\n' 'delegated_by delegated_to accepted_by scope starts_on ends_on' ;;
    'enforcement_context') printf '%s\n' 'enforcement_class blast_radius_class checker_result exceptions' ;;
    'historical_event_reference') printf '%s\n' 'kind event_id source_path reviewed_revision digest_algorithm content_digest' ;;
    'manifest_result') printf '%s\n' 'project_status current_gate active_role phase_position current_phase_id latest_decision_event_id manifest_digest' ;;
    'next_state') printf '%s\n' 'project_status current_gate active_role phase_position next_artifact_id' ;;
    'operational_owner_confirmation') printf '%s\n' 'confirmed_by confirmed_on checked_statement' ;;
    'operational_results') printf '%s\n' 'production_action_performed rollback_status incident_status monitoring_status evidence_event_ids' ;;
    'regression_result') printf '%s\n' 'candidate_revision commands result phases_covered' ;;
    'risk_disposition') printf '%s\n' 'disposition rationale accepted_by accepted_on owner target_date' ;;
    'security_approval') printf '%s\n' 'required decision approver approved_on checked_statement basis' ;;
    'test_uat_execution') printf '%s\n' 'candidate_revision command_results evidence_event_ids' ;;
    'command_result_item') printf '%s\n' 'command result' ;;
    'value_review') printf '%s\n' 'disposition details' ;;
    'value_review_details') printf '%s\n' 'criterion_results trigger expected_date_when_knowable owner evidence_source next_review_mechanism rationale accepted_by accepted_on' ;;
    'value_result_item') printf '%s\n' 'criterion_id evidence result follow_up_decision' ;;
    'reconciliation_artifact_item') printf '%s\n' 'artifact_id artifact_path status action evidence_event_id' ;;
    'residual_finding_item') printf '%s\n' 'finding_id severity disposition owner rationale' ;;
    'migration_provenance_item') printf '%s\n' 'artifact_path reviewed_revision blob_oid digest' ;;
    'enforcement_exception_item') printf '%s\n' 'requirement_id disposition rationale approved_by approved_on' ;;
    *) return 1 ;;
  esac
}

gendev_event_record_conditional_profiles() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'event_evidence_item') printf '%s\n' 'accepted_authority_unchanged' ;;
    'event_reference_item') printf '%s\n' '' ;;
    'coverage_result') printf '%s\n' '' ;;
    'delegation') printf '%s\n' '' ;;
    'enforcement_context') printf '%s\n' '' ;;
    'historical_event_reference') printf '%s\n' 'stable_event_id content_digest' ;;
    'manifest_result') printf '%s\n' '' ;;
    'next_state') printf '%s\n' '' ;;
    'operational_owner_confirmation') printf '%s\n' '' ;;
    'operational_results') printf '%s\n' '' ;;
    'regression_result') printf '%s\n' '' ;;
    'risk_disposition') printf '%s\n' 'accepted follow_up' ;;
    'security_approval') printf '%s\n' 'required not_required' ;;
    'test_uat_execution') printf '%s\n' '' ;;
    'command_result_item') printf '%s\n' '' ;;
    'value_review') printf '%s\n' '' ;;
    'value_review_details') printf '%s\n' 'complete not_due not_applicable' ;;
    'value_result_item') printf '%s\n' 'missed unmeasurable' ;;
    'reconciliation_artifact_item') printf '%s\n' '' ;;
    'residual_finding_item') printf '%s\n' '' ;;
    'migration_provenance_item') printf '%s\n' '' ;;
    'enforcement_exception_item') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

gendev_event_record_conditional_fields() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'event_evidence_item:accepted_authority_unchanged') printf '%s\n' 'originating_event_id' ;;
    'historical_event_reference:stable_event_id') printf '%s\n' 'event_id' ;;
    'historical_event_reference:content_digest') printf '%s\n' 'source_path reviewed_revision digest_algorithm content_digest' ;;
    'risk_disposition:accepted') printf '%s\n' 'accepted_by accepted_on' ;;
    'risk_disposition:follow_up') printf '%s\n' 'owner target_date' ;;
    'security_approval:required') printf '%s\n' 'approver approved_on checked_statement' ;;
    'security_approval:not_required') printf '%s\n' 'basis' ;;
    'value_review_details:complete') printf '%s\n' 'criterion_results' ;;
    'value_review_details:not_due') printf '%s\n' 'trigger expected_date_when_knowable owner evidence_source next_review_mechanism' ;;
    'value_review_details:not_applicable') printf '%s\n' 'rationale accepted_by accepted_on' ;;
    'value_result_item:missed') printf '%s\n' 'follow_up_decision' ;;
    'value_result_item:unmeasurable') printf '%s\n' 'follow_up_decision' ;;
    *) return 1 ;;
  esac
}

gendev_event_record_field_shape() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'event_evidence_item:artifact_id') printf '%s\n' 'scalar' ;;
    'event_evidence_item:artifact_path') printf '%s\n' 'scalar' ;;
    'event_evidence_item:category') printf '%s\n' 'scalar' ;;
    'event_evidence_item:reviewed_revision') printf '%s\n' 'scalar' ;;
    'event_evidence_item:reviewed_blob_oid') printf '%s\n' 'scalar' ;;
    'event_evidence_item:reviewed_digest') printf '%s\n' 'scalar' ;;
    'event_evidence_item:resulting_blob_oid') printf '%s\n' 'scalar' ;;
    'event_evidence_item:resulting_digest') printf '%s\n' 'scalar' ;;
    'event_evidence_item:status') printf '%s\n' 'scalar' ;;
    'event_evidence_item:originating_event_id') printf '%s\n' 'scalar' ;;
    'event_reference_item:artifact_id') printf '%s\n' 'scalar' ;;
    'event_reference_item:artifact_path') printf '%s\n' 'scalar' ;;
    'event_reference_item:status') printf '%s\n' 'scalar' ;;
    'event_reference_item:revision') printf '%s\n' 'scalar' ;;
    'event_reference_item:blob_oid') printf '%s\n' 'scalar' ;;
    'event_reference_item:digest') printf '%s\n' 'scalar' ;;
    'coverage_result:policy_reference') printf '%s\n' 'scalar' ;;
    'coverage_result:metric') printf '%s\n' 'scalar' ;;
    'coverage_result:measurement_tool') printf '%s\n' 'scalar' ;;
    'coverage_result:denominator') printf '%s\n' 'scalar' ;;
    'coverage_result:target') printf '%s\n' 'scalar' ;;
    'coverage_result:actual') printf '%s\n' 'scalar' ;;
    'coverage_result:result') printf '%s\n' 'scalar' ;;
    'coverage_result:scope') printf '%s\n' 'scalar' ;;
    'coverage_result:measurement_command') printf '%s\n' 'scalar' ;;
    'coverage_result:exclusions') printf '%s\n' 'scalar_list' ;;
    'coverage_result:exclusion_rationale') printf '%s\n' 'scalar' ;;
    'coverage_result:owner') printf '%s\n' 'scalar' ;;
    'coverage_result:shortfall_disposition') printf '%s\n' 'scalar' ;;
    'delegation:delegated_by') printf '%s\n' 'scalar' ;;
    'delegation:delegated_to') printf '%s\n' 'scalar' ;;
    'delegation:accepted_by') printf '%s\n' 'scalar' ;;
    'delegation:scope') printf '%s\n' 'scalar' ;;
    'delegation:starts_on') printf '%s\n' 'scalar' ;;
    'delegation:ends_on') printf '%s\n' 'scalar' ;;
    'enforcement_context:enforcement_class') printf '%s\n' 'scalar' ;;
    'enforcement_context:blast_radius_class') printf '%s\n' 'scalar' ;;
    'enforcement_context:checker_result') printf '%s\n' 'scalar' ;;
    'enforcement_context:exceptions') printf '%s\n' 'scalar_list' ;;
    'historical_event_reference:kind') printf '%s\n' 'scalar' ;;
    'historical_event_reference:event_id') printf '%s\n' 'scalar' ;;
    'historical_event_reference:source_path') printf '%s\n' 'scalar' ;;
    'historical_event_reference:reviewed_revision') printf '%s\n' 'scalar' ;;
    'historical_event_reference:digest_algorithm') printf '%s\n' 'scalar' ;;
    'historical_event_reference:content_digest') printf '%s\n' 'scalar' ;;
    'manifest_result:project_status') printf '%s\n' 'scalar' ;;
    'manifest_result:current_gate') printf '%s\n' 'scalar' ;;
    'manifest_result:active_role') printf '%s\n' 'scalar' ;;
    'manifest_result:phase_position') printf '%s\n' 'scalar' ;;
    'manifest_result:current_phase_id') printf '%s\n' 'scalar' ;;
    'manifest_result:latest_decision_event_id') printf '%s\n' 'scalar' ;;
    'manifest_result:manifest_digest') printf '%s\n' 'scalar' ;;
    'next_state:project_status') printf '%s\n' 'scalar' ;;
    'next_state:current_gate') printf '%s\n' 'scalar' ;;
    'next_state:active_role') printf '%s\n' 'scalar' ;;
    'next_state:phase_position') printf '%s\n' 'scalar' ;;
    'next_state:next_artifact_id') printf '%s\n' 'scalar' ;;
    'operational_owner_confirmation:confirmed_by') printf '%s\n' 'scalar' ;;
    'operational_owner_confirmation:confirmed_on') printf '%s\n' 'scalar' ;;
    'operational_owner_confirmation:checked_statement') printf '%s\n' 'scalar' ;;
    'operational_results:production_action_performed') printf '%s\n' 'scalar' ;;
    'operational_results:rollback_status') printf '%s\n' 'scalar' ;;
    'operational_results:incident_status') printf '%s\n' 'scalar' ;;
    'operational_results:monitoring_status') printf '%s\n' 'scalar' ;;
    'operational_results:evidence_event_ids') printf '%s\n' 'scalar_list' ;;
    'regression_result:candidate_revision') printf '%s\n' 'scalar' ;;
    'regression_result:commands') printf '%s\n' 'scalar_list' ;;
    'regression_result:result') printf '%s\n' 'scalar' ;;
    'regression_result:phases_covered') printf '%s\n' 'scalar_list' ;;
    'risk_disposition:disposition') printf '%s\n' 'scalar' ;;
    'risk_disposition:rationale') printf '%s\n' 'scalar' ;;
    'risk_disposition:accepted_by') printf '%s\n' 'scalar' ;;
    'risk_disposition:accepted_on') printf '%s\n' 'scalar' ;;
    'risk_disposition:owner') printf '%s\n' 'scalar' ;;
    'risk_disposition:target_date') printf '%s\n' 'scalar' ;;
    'security_approval:required') printf '%s\n' 'scalar' ;;
    'security_approval:decision') printf '%s\n' 'scalar' ;;
    'security_approval:approver') printf '%s\n' 'scalar' ;;
    'security_approval:approved_on') printf '%s\n' 'scalar' ;;
    'security_approval:checked_statement') printf '%s\n' 'scalar' ;;
    'security_approval:basis') printf '%s\n' 'scalar' ;;
    'test_uat_execution:candidate_revision') printf '%s\n' 'scalar' ;;
    'test_uat_execution:command_results') printf '%s\n' 'record_list' ;;
    'test_uat_execution:evidence_event_ids') printf '%s\n' 'scalar_list' ;;
    'command_result_item:command') printf '%s\n' 'scalar' ;;
    'command_result_item:result') printf '%s\n' 'scalar' ;;
    'value_review:disposition') printf '%s\n' 'scalar' ;;
    'value_review:details') printf '%s\n' 'record' ;;
    'value_review_details:criterion_results') printf '%s\n' 'record_list' ;;
    'value_review_details:trigger') printf '%s\n' 'scalar' ;;
    'value_review_details:expected_date_when_knowable') printf '%s\n' 'scalar' ;;
    'value_review_details:owner') printf '%s\n' 'scalar' ;;
    'value_review_details:evidence_source') printf '%s\n' 'scalar' ;;
    'value_review_details:next_review_mechanism') printf '%s\n' 'scalar' ;;
    'value_review_details:rationale') printf '%s\n' 'scalar' ;;
    'value_review_details:accepted_by') printf '%s\n' 'scalar' ;;
    'value_review_details:accepted_on') printf '%s\n' 'scalar' ;;
    'value_result_item:criterion_id') printf '%s\n' 'scalar' ;;
    'value_result_item:evidence') printf '%s\n' 'scalar_list' ;;
    'value_result_item:result') printf '%s\n' 'scalar' ;;
    'value_result_item:follow_up_decision') printf '%s\n' 'scalar' ;;
    'reconciliation_artifact_item:artifact_id') printf '%s\n' 'scalar' ;;
    'reconciliation_artifact_item:artifact_path') printf '%s\n' 'scalar' ;;
    'reconciliation_artifact_item:status') printf '%s\n' 'scalar' ;;
    'reconciliation_artifact_item:action') printf '%s\n' 'scalar' ;;
    'reconciliation_artifact_item:evidence_event_id') printf '%s\n' 'scalar' ;;
    'residual_finding_item:finding_id') printf '%s\n' 'scalar' ;;
    'residual_finding_item:severity') printf '%s\n' 'scalar' ;;
    'residual_finding_item:disposition') printf '%s\n' 'scalar' ;;
    'residual_finding_item:owner') printf '%s\n' 'scalar' ;;
    'residual_finding_item:rationale') printf '%s\n' 'scalar' ;;
    'migration_provenance_item:artifact_path') printf '%s\n' 'scalar' ;;
    'migration_provenance_item:reviewed_revision') printf '%s\n' 'scalar' ;;
    'migration_provenance_item:blob_oid') printf '%s\n' 'scalar' ;;
    'migration_provenance_item:digest') printf '%s\n' 'scalar' ;;
    'enforcement_exception_item:requirement_id') printf '%s\n' 'scalar' ;;
    'enforcement_exception_item:disposition') printf '%s\n' 'scalar' ;;
    'enforcement_exception_item:rationale') printf '%s\n' 'scalar' ;;
    'enforcement_exception_item:approved_by') printf '%s\n' 'scalar' ;;
    'enforcement_exception_item:approved_on') printf '%s\n' 'scalar' ;;
    *) return 1 ;;
  esac
}

gendev_event_record_field_item_contract() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'test_uat_execution:command_results') printf '%s\n' 'command_result_item' ;;
    'value_review:details') printf '%s\n' 'value_review_details' ;;
    'value_review_details:criterion_results') printf '%s\n' 'value_result_item' ;;
    *) return 1 ;;
  esac
}

gendev_event_record_field_min_items() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'operational_results:evidence_event_ids') printf '%s\n' '1' ;;
    'regression_result:commands') printf '%s\n' '1' ;;
    'regression_result:phases_covered') printf '%s\n' '1' ;;
    'test_uat_execution:command_results') printf '%s\n' '1' ;;
    'test_uat_execution:evidence_event_ids') printf '%s\n' '1' ;;
    'value_review_details:criterion_results') printf '%s\n' '1' ;;
    'value_result_item:evidence') printf '%s\n' '1' ;;
    *) return 1 ;;
  esac
}

gendev_event_record_field_value_contract() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'event_evidence_item:artifact_id') printf '%s\n' 'artifact_id' ;;
    'event_evidence_item:artifact_path') printf '%s\n' 'canonical_artifact_path' ;;
    'event_evidence_item:category') printf '%s\n' 'evidence_category' ;;
    'event_evidence_item:reviewed_revision') printf '%s\n' 'git_revision' ;;
    'event_evidence_item:reviewed_blob_oid') printf '%s\n' 'git_blob_oid' ;;
    'event_evidence_item:reviewed_digest') printf '%s\n' 'sha256' ;;
    'event_evidence_item:resulting_blob_oid') printf '%s\n' 'git_blob_oid' ;;
    'event_evidence_item:resulting_digest') printf '%s\n' 'sha256' ;;
    'event_evidence_item:status') printf '%s\n' 'artifact_status' ;;
    'event_evidence_item:originating_event_id') printf '%s\n' 'event_id_pattern' ;;
    'event_reference_item:artifact_id') printf '%s\n' 'artifact_id' ;;
    'event_reference_item:artifact_path') printf '%s\n' 'canonical_artifact_path' ;;
    'event_reference_item:status') printf '%s\n' 'artifact_status' ;;
    'event_reference_item:revision') printf '%s\n' 'git_revision' ;;
    'event_reference_item:blob_oid') printf '%s\n' 'git_blob_oid' ;;
    'event_reference_item:digest') printf '%s\n' 'sha256' ;;
    'coverage_result:policy_reference') printf '%s\n' 'nonempty_string' ;;
    'coverage_result:metric') printf '%s\n' 'nonempty_string' ;;
    'coverage_result:measurement_tool') printf '%s\n' 'nonempty_string' ;;
    'coverage_result:denominator') printf '%s\n' 'nonempty_string' ;;
    'coverage_result:target') printf '%s\n' 'nonempty_string' ;;
    'coverage_result:actual') printf '%s\n' 'nonempty_string' ;;
    'coverage_result:result') printf '%s\n' 'nonempty_string' ;;
    'coverage_result:scope') printf '%s\n' 'nonempty_string' ;;
    'coverage_result:measurement_command') printf '%s\n' 'nonempty_string' ;;
    'coverage_result:exclusion_rationale') printf '%s\n' 'nonempty_string' ;;
    'coverage_result:owner') printf '%s\n' 'nonempty_string' ;;
    'coverage_result:shortfall_disposition') printf '%s\n' 'nonempty_string' ;;
    'delegation:delegated_by') printf '%s\n' 'nonempty_string' ;;
    'delegation:delegated_to') printf '%s\n' 'nonempty_string' ;;
    'delegation:accepted_by') printf '%s\n' 'nonempty_string' ;;
    'delegation:scope') printf '%s\n' 'nonempty_string' ;;
    'delegation:starts_on') printf '%s\n' 'iso_date' ;;
    'delegation:ends_on') printf '%s\n' 'iso_date' ;;
    'enforcement_context:enforcement_class') printf '%s\n' 'enforcement_class' ;;
    'enforcement_context:blast_radius_class') printf '%s\n' 'blast_radius_class' ;;
    'enforcement_context:checker_result') printf '%s\n' 'nonempty_string' ;;
    'historical_event_reference:kind') printf '%s\n' 'migration_reference_kind' ;;
    'historical_event_reference:event_id') printf '%s\n' 'event_id_pattern' ;;
    'historical_event_reference:source_path') printf '%s\n' 'nonempty_string' ;;
    'historical_event_reference:reviewed_revision') printf '%s\n' 'git_revision' ;;
    'historical_event_reference:digest_algorithm') printf '%s\n' 'fixed:sha256' ;;
    'historical_event_reference:content_digest') printf '%s\n' 'sha256' ;;
    'manifest_result:project_status') printf '%s\n' 'project_status' ;;
    'manifest_result:current_gate') printf '%s\n' 'gate_id' ;;
    'manifest_result:active_role') printf '%s\n' 'role_id' ;;
    'manifest_result:phase_position') printf '%s\n' 'checkpoint_position_or_null' ;;
    'manifest_result:current_phase_id') printf '%s\n' 'phase_id_or_null' ;;
    'manifest_result:latest_decision_event_id') printf '%s\n' 'event_id_pattern_or_null' ;;
    'manifest_result:manifest_digest') printf '%s\n' 'sha256' ;;
    'next_state:project_status') printf '%s\n' 'project_status' ;;
    'next_state:current_gate') printf '%s\n' 'gate_id' ;;
    'next_state:active_role') printf '%s\n' 'role_id' ;;
    'next_state:phase_position') printf '%s\n' 'checkpoint_position_or_null' ;;
    'next_state:next_artifact_id') printf '%s\n' 'artifact_id_or_null' ;;
    'operational_owner_confirmation:confirmed_by') printf '%s\n' 'nonempty_string' ;;
    'operational_owner_confirmation:confirmed_on') printf '%s\n' 'iso_date' ;;
    'operational_owner_confirmation:checked_statement') printf '%s\n' 'nonempty_string' ;;
    'operational_results:production_action_performed') printf '%s\n' 'boolean' ;;
    'operational_results:rollback_status') printf '%s\n' 'nonempty_string' ;;
    'operational_results:incident_status') printf '%s\n' 'nonempty_string' ;;
    'operational_results:monitoring_status') printf '%s\n' 'nonempty_string' ;;
    'regression_result:candidate_revision') printf '%s\n' 'git_revision' ;;
    'regression_result:result') printf '%s\n' 'nonempty_string' ;;
    'risk_disposition:disposition') printf '%s\n' 'nonempty_string' ;;
    'risk_disposition:rationale') printf '%s\n' 'nonempty_string' ;;
    'risk_disposition:accepted_by') printf '%s\n' 'nonempty_string' ;;
    'risk_disposition:accepted_on') printf '%s\n' 'iso_date' ;;
    'risk_disposition:owner') printf '%s\n' 'nonempty_string' ;;
    'risk_disposition:target_date') printf '%s\n' 'iso_date' ;;
    'security_approval:required') printf '%s\n' 'boolean' ;;
    'security_approval:decision') printf '%s\n' 'approval_decision' ;;
    'security_approval:approver') printf '%s\n' 'nonempty_string' ;;
    'security_approval:approved_on') printf '%s\n' 'iso_date' ;;
    'security_approval:checked_statement') printf '%s\n' 'nonempty_string' ;;
    'security_approval:basis') printf '%s\n' 'nonempty_string' ;;
    'test_uat_execution:candidate_revision') printf '%s\n' 'git_revision' ;;
    'command_result_item:command') printf '%s\n' 'nonempty_string' ;;
    'command_result_item:result') printf '%s\n' 'nonempty_string' ;;
    'value_review:disposition') printf '%s\n' 'value_review_disposition' ;;
    'value_review_details:trigger') printf '%s\n' 'nonempty_string' ;;
    'value_review_details:expected_date_when_knowable') printf '%s\n' 'nonempty_string' ;;
    'value_review_details:owner') printf '%s\n' 'nonempty_string' ;;
    'value_review_details:evidence_source') printf '%s\n' 'nonempty_string' ;;
    'value_review_details:next_review_mechanism') printf '%s\n' 'nonempty_string' ;;
    'value_review_details:rationale') printf '%s\n' 'nonempty_string' ;;
    'value_review_details:accepted_by') printf '%s\n' 'nonempty_string' ;;
    'value_review_details:accepted_on') printf '%s\n' 'nonempty_string' ;;
    'value_result_item:criterion_id') printf '%s\n' 'nonempty_string' ;;
    'value_result_item:result') printf '%s\n' 'value_result' ;;
    'value_result_item:follow_up_decision') printf '%s\n' 'nonempty_string' ;;
    'reconciliation_artifact_item:artifact_id') printf '%s\n' 'nonempty_string' ;;
    'reconciliation_artifact_item:artifact_path') printf '%s\n' 'nonempty_string' ;;
    'reconciliation_artifact_item:status') printf '%s\n' 'nonempty_string' ;;
    'reconciliation_artifact_item:action') printf '%s\n' 'nonempty_string' ;;
    'reconciliation_artifact_item:evidence_event_id') printf '%s\n' 'nonempty_string' ;;
    'residual_finding_item:finding_id') printf '%s\n' 'nonempty_string' ;;
    'residual_finding_item:severity') printf '%s\n' 'nonempty_string' ;;
    'residual_finding_item:disposition') printf '%s\n' 'nonempty_string' ;;
    'residual_finding_item:owner') printf '%s\n' 'nonempty_string' ;;
    'residual_finding_item:rationale') printf '%s\n' 'nonempty_string' ;;
    'migration_provenance_item:artifact_path') printf '%s\n' 'nonempty_string' ;;
    'migration_provenance_item:reviewed_revision') printf '%s\n' 'git_revision' ;;
    'migration_provenance_item:blob_oid') printf '%s\n' 'git_blob_oid' ;;
    'migration_provenance_item:digest') printf '%s\n' 'sha256' ;;
    'enforcement_exception_item:requirement_id') printf '%s\n' 'nonempty_string' ;;
    'enforcement_exception_item:disposition') printf '%s\n' 'nonempty_string' ;;
    'enforcement_exception_item:rationale') printf '%s\n' 'nonempty_string' ;;
    'enforcement_exception_item:approved_by') printf '%s\n' 'nonempty_string' ;;
    'enforcement_exception_item:approved_on') printf '%s\n' 'iso_date' ;;
    *) return 1 ;;
  esac
}

gendev_event_record_conditional_profile_selector() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'event_evidence_item') printf '%s\n' 'category' ;;
    'historical_event_reference') printf '%s\n' 'kind' ;;
    'risk_disposition') printf '%s\n' 'disposition' ;;
    'security_approval') printf '%s\n' 'required' ;;
    'value_review_details') printf '%s\n' 'parent.disposition' ;;
    'value_result_item') printf '%s\n' 'result' ;;
    *) return 1 ;;
  esac
}

gendev_event_record_conditional_profile_for_value() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'security_approval:true') printf '%s\n' 'required' ;;
    'security_approval:false') printf '%s\n' 'not_required' ;;
    *) return 1 ;;
  esac
}

gendev_migration_reference_required_fields() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'stable_event_id') printf '%s\n' 'event_id' ;;
    'content_digest') printf '%s\n' 'source_path reviewed_revision digest_algorithm content_digest' ;;
    *) return 1 ;;
  esac
}

gendev_event_history_string() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'template_projection') printf '%s\n' 'docs/methodology/templates/gate-log-template.md' ;;
    'template_projection_state') printf '%s\n' 'planned' ;;
    'required_work_package') printf '%s\n' 'WP-04' ;;
    *) return 1 ;;
  esac
}

gendev_event_history_boolean() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'append_only') printf '%s\n' 'true' ;;
    'corrections_are_new_events') printf '%s\n' 'true' ;;
    'duplicate_latest_is_invalid') printf '%s\n' 'true' ;;
    'supersession_cycles_are_invalid') printf '%s\n' 'true' ;;
    'prior_event_text_must_remain_byte_identical') printf '%s\n' 'true' ;;
    *) return 1 ;;
  esac
}

gendev_event_history_enforcement_string() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'state') printf '%s\n' 'planned' ;;
    'required_work_package') printf '%s\n' 'WP-05' ;;
    'enforcer_path') printf '%s\n' 'scripts/lib/gate-log.awk' ;;
    'verification_suite') printf '%s\n' 'tests/methodology/test-enforcement-tools.sh' ;;
    *) return 1 ;;
  esac
}

gendev_reference_depth_rule() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'greater_depth_requires_exception') printf '%s\n' 'true' ;;
    'supporting_to_supporting_reference_allowed_only_by_exception') printf '%s\n' 'true' ;;
    *) return 1 ;;
  esac
}

gendev_manifest_field_contract_state() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'phase.loop_status') printf '%s\n' 'planned' ;;
    'phase.current_phase_id') printf '%s\n' 'planned' ;;
    *) return 1 ;;
  esac
}

gendev_manifest_field_required_work_package() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'phase.loop_status') printf '%s\n' 'WP-04' ;;
    'phase.current_phase_id') printf '%s\n' 'WP-04' ;;
    *) return 1 ;;
  esac
}

gendev_manifest_invariant_string() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'MANIFEST-GATE-MAJOR:rule') printf '%s\n' 'project.current_gate is exactly one declared G0-G9 ID.' ;;
    'MANIFEST-PHASE-AXIS:rule') printf '%s\n' 'phase.phase_position is null before G5, records the most recently completed checkpoint inside G5, and retains the final phase position after the loop closes; it never changes project.current_gate.' ;;
    'MANIFEST-PHASE-AXIS:before_g5') printf '%s\n' 'null' ;;
    'MANIFEST-PHASE-AXIS:inside_g5') printf '%s\n' 'latest_completed_checkpoint' ;;
    'MANIFEST-PHASE-MEMBER:rule') printf '%s\n' 'phase.current_phase_id is null or names exactly one declared phase.' ;;
    'MANIFEST-PHASE-ORDER:rule') printf '%s\n' 'Phase array order is authoritative and IDs are unique.' ;;
    'MANIFEST-LATEST-SUMMARY:rule') printf '%s\n' 'approvals.latest_decision summarizes but never replaces a gate-log event.' ;;
    'MANIFEST-TERMINAL:rule') printf '%s\n' 'G9 requires project.status closed, active_role none, and no next gate, role, or artifact.' ;;
    'MANIFEST-TERMINAL:current_gate') printf '%s\n' 'G9' ;;
    'MANIFEST-TERMINAL:project_status') printf '%s\n' 'closed' ;;
    'MANIFEST-TERMINAL:active_role') printf '%s\n' 'none' ;;
    'MANIFEST-STRICT-V2:rule') printf '%s\n' 'New 0.5 projects use strict schema version 2; legacy mode is explicit and version-bound.' ;;
    *) return 1 ;;
  esac
}

gendev_manifest_invariant_boolean() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'MANIFEST-PHASE-AXIS:retained_final_position') printf '%s\n' 'true' ;;
    'MANIFEST-PHASE-AXIS:major_gate_independent') printf '%s\n' 'true' ;;
    'MANIFEST-TERMINAL:next_gate_must_be_null') printf '%s\n' 'true' ;;
    'MANIFEST-TERMINAL:next_role_must_be_null') printf '%s\n' 'true' ;;
    'MANIFEST-TERMINAL:next_artifact_must_be_null') printf '%s\n' 'true' ;;
    *) return 1 ;;
  esac
}

gendev_deployment_path_string() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'deploy:deployment_record_status') printf '%s\n' 'Complete' ;;
    'deploy:terminal_disposition') printf '%s\n' 'deployed' ;;
    'non_deployment:deployment_record_status') printf '%s\n' 'Complete' ;;
    'non_deployment:terminal_disposition') printf '%s\n' 'not_deployed' ;;
    *) return 1 ;;
  esac
}

gendev_deployment_path_boolean() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'deploy:authorization_required_before_action') printf '%s\n' 'true' ;;
    'deploy:operational_owner_confirmation_required') printf '%s\n' 'true' ;;
    'deploy:production_action_performed') printf '%s\n' 'true' ;;
    'non_deployment:allowed') printf '%s\n' 'true' ;;
    'non_deployment:authorization_required') printf '%s\n' 'true' ;;
    'non_deployment:production_action_performed') printf '%s\n' 'false' ;;
    *) return 1 ;;
  esac
}

gendev_deployment_path_fields() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'non_deployment:required_fields') printf '%s\n' 'disposition rationale scope release_candidate approver approved_on future_trigger_or_finality' ;;
    *) return 1 ;;
  esac
}

readonly GENDEV_DEPLOYMENT_PRODUCTION_ACTION_AUTOMATIC='false' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_DEPLOYMENT_PRODUCTION_ACTION_AUTOMATIC' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_DEPLOYMENT_VALUE_MUST_BE_COMPLETE_BEFORE_AUTHORIZATION='true' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_DEPLOYMENT_VALUE_MUST_BE_COMPLETE_BEFORE_AUTHORIZATION' >&2
  return 2 2>/dev/null || exit 2
}
readonly GENDEV_DEPLOYMENT_INTENT_MUST_MATCH_TERMINAL_DISPOSITION='true' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_DEPLOYMENT_INTENT_MUST_MATCH_TERMINAL_DISPOSITION' >&2
  return 2 2>/dev/null || exit 2
}

gendev_role_kind() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'none') printf '%s\n' 'terminal' ;;
    'product-vision-agent') printf '%s\n' 'authoring' ;;
    'prd-agent') printf '%s\n' 'authoring' ;;
    'architecture-agent') printf '%s\n' 'authoring' ;;
    'security-governance-agent') printf '%s\n' 'authoring' ;;
    'phase-planning-agent') printf '%s\n' 'authoring' ;;
    'implementation-agent') printf '%s\n' 'implementation' ;;
    'test-uat-agent') printf '%s\n' 'verification' ;;
    'code-review-agent') printf '%s\n' 'independent_review' ;;
    'remediation-agent') printf '%s\n' 'remediation' ;;
    'deployment-readiness-agent') printf '%s\n' 'operations' ;;
    'as-built-closeout-agent') printf '%s\n' 'closeout' ;;
    'named-human-approver') printf '%s\n' 'human_approval' ;;
    'named-human-deployment-approver') printf '%s\n' 'human_approval' ;;
    'named-human-security-approver') printf '%s\n' 'human_approval' ;;
    'named-human-operational-owner') printf '%s\n' 'human_confirmation' ;;
    *) return 1 ;;
  esac
}

gendev_role_may_approve() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'none') printf '%s\n' 'false' ;;
    'product-vision-agent') printf '%s\n' 'false' ;;
    'prd-agent') printf '%s\n' 'false' ;;
    'architecture-agent') printf '%s\n' 'false' ;;
    'security-governance-agent') printf '%s\n' 'false' ;;
    'phase-planning-agent') printf '%s\n' 'false' ;;
    'implementation-agent') printf '%s\n' 'false' ;;
    'test-uat-agent') printf '%s\n' 'false' ;;
    'code-review-agent') printf '%s\n' 'false' ;;
    'remediation-agent') printf '%s\n' 'false' ;;
    'deployment-readiness-agent') printf '%s\n' 'false' ;;
    'as-built-closeout-agent') printf '%s\n' 'false' ;;
    'named-human-approver') printf '%s\n' 'true' ;;
    'named-human-deployment-approver') printf '%s\n' 'true' ;;
    'named-human-security-approver') printf '%s\n' 'true' ;;
    'named-human-operational-owner') printf '%s\n' 'false' ;;
    *) return 1 ;;
  esac
}

gendev_gate_name() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G0') printf '%s\n' 'Project Initialized' ;;
    'G1') printf '%s\n' 'Vision Ready' ;;
    'G2') printf '%s\n' 'Requirements Ready' ;;
    'G3') printf '%s\n' 'Architecture Ready' ;;
    'G4') printf '%s\n' 'Governance Ready' ;;
    'G5') printf '%s\n' 'Build Ready' ;;
    'G6') printf '%s\n' 'Implementation Ready For Review' ;;
    'G7') printf '%s\n' 'Acceptance Ready' ;;
    'G8') printf '%s\n' 'Deployment Ready' ;;
    'G9') printf '%s\n' 'As-Built Closed' ;;
    *) return 1 ;;
  esac
}

gendev_gate_successor() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G0') printf '%s\n' 'G1' ;;
    'G1') printf '%s\n' 'G2' ;;
    'G2') printf '%s\n' 'G3' ;;
    'G3') printf '%s\n' 'G4' ;;
    'G4') printf '%s\n' 'G5' ;;
    'G5') printf '%s\n' 'G6' ;;
    'G6') printf '%s\n' 'G7' ;;
    'G7') printf '%s\n' 'G8' ;;
    'G8') printf '%s\n' 'G9' ;;
    'G9') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

gendev_gate_role() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G0') printf '%s\n' 'product-vision-agent' ;;
    'G1') printf '%s\n' 'product-vision-agent' ;;
    'G2') printf '%s\n' 'prd-agent' ;;
    'G3') printf '%s\n' 'architecture-agent' ;;
    'G4') printf '%s\n' 'security-governance-agent' ;;
    'G5') printf '%s\n' 'phase-planning-agent' ;;
    'G6') printf '%s\n' 'implementation-agent' ;;
    'G7') printf '%s\n' 'code-review-agent' ;;
    'G8') printf '%s\n' 'deployment-readiness-agent' ;;
    'G9') printf '%s\n' 'none' ;;
    *) return 1 ;;
  esac
}

gendev_gate_primary_artifact() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G0') printf '%s\n' 'project_manifest' ;;
    'G1') printf '%s\n' 'vision' ;;
    'G2') printf '%s\n' 'prd' ;;
    'G3') printf '%s\n' 'architecture' ;;
    'G4') printf '%s\n' 'governance_security' ;;
    'G5') printf '%s\n' 'phase_plan' ;;
    'G6') printf '%s\n' 'implementation_summary' ;;
    'G7') printf '%s\n' 'final_code_review' ;;
    'G8') printf '%s\n' 'deployment_readiness' ;;
    'G9') printf '%s\n' 'project_as_built' ;;
    *) return 1 ;;
  esac
}

gendev_gate_approval() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G0') printf '%s\n' 'conditional_overwrite_only' ;;
    'G1') printf '%s\n' 'required' ;;
    'G2') printf '%s\n' 'required' ;;
    'G3') printf '%s\n' 'required' ;;
    'G4') printf '%s\n' 'required' ;;
    'G5') printf '%s\n' 'each_authority_acceptance_checkpoint_and_phase_exit' ;;
    'G6') printf '%s\n' 'conditional_skipped_critical_verification_or_nonwaivable_residual' ;;
    'G7') printf '%s\n' 'required_implementation_acceptance' ;;
    'G8') printf '%s\n' 'required_deployment_and_closeout' ;;
    'G9') printf '%s\n' 'required_project_closeout' ;;
    *) return 1 ;;
  esac
}

gendev_gate_criteria() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G0') printf '%s\n' 'G0-MANIFEST G0-SCAFFOLD G0-IDENTITY' ;;
    'G1') printf '%s\n' 'G1-PROBLEM G1-USERS G1-SUCCESS G1-NON-GOALS G1-RISKS-QUESTIONS' ;;
    'G2') printf '%s\n' 'G2-REQ-ID G2-AC-COVERAGE G2-EARS-FORM G2-OBSERVABLE-FORM G2-UNWANTED-BEHAVIOR G2-TESTABILITY' ;;
    'G3') printf '%s\n' 'G3-ARCH-TRACE G3-BOUNDARIES G3-STATE-LIFECYCLE G3-STACK-ACCEPTED G3-VERIFICATION-SPEC G3-VERIFICATION-TRACE G3-DESIGN-INTERROGATION' ;;
    'G4') printf '%s\n' 'G4-ACTOR-MODEL G4-AUTHORIZATION G4-AUDIT G4-SECRETS-DATA G4-TRUST-TOOLS G4-NEGATIVE-TESTS' ;;
    'G5') printf '%s\n' 'G5-PHASE-ORDER G5-REQ-COVERAGE G5-INTEGRATION G5-PARTITION G5-COVERAGE-POLICY' ;;
    'G6') printf '%s\n' 'G6-IMPLEMENTATION-SUMMARY G6-CANDIDATE-REVISION G6-CHANGED-FILES G6-INTEGRATION-REGRESSION G6-DEVIATIONS-RESIDUALS G6-REVIEW-PACKAGE' ;;
    'G7') printf '%s\n' 'G7-INDEPENDENT-REVIEW G7-REMEDIATION G7-FINAL-UAT G7-TRACEABILITY G7-RESIDUAL-RISK G7-UPSTREAM-BINDINGS' ;;
    'G8') printf '%s\n' 'G8-READINESS G8-RUNBOOK G8-RELEASE-SCOPE G8-CONFIG-SECRETS G8-MIGRATION-ROLLBACK G8-MONITORING-OWNERSHIP G8-VALUE-PREREQUISITES G8-DEPLOYMENT-APPROVAL' ;;
    'G9') printf '%s\n' 'G9-DEPLOYMENT-DISPOSITION G9-OPERATIONS-RESULTS G9-VALUE-DISPOSITION G9-FINAL-TRACEABILITY G9-AS-BUILT-CLOSEOUT' ;;
    *) return 1 ;;
  esac
}

gendev_transition_event_type() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G0:G1') printf '%s\n' 'project_initialization' ;;
    'G1:G2') printf '%s\n' 'gate_transition' ;;
    'G2:G3') printf '%s\n' 'gate_transition' ;;
    'G3:G4') printf '%s\n' 'gate_transition' ;;
    'G4:G5') printf '%s\n' 'gate_transition' ;;
    'G5:G6') printf '%s\n' 'gate_transition' ;;
    'G6:G7') printf '%s\n' 'gate_transition' ;;
    'G7:G8') printf '%s\n' 'gate_transition' ;;
    'G8:G9') printf '%s\n' 'gate_transition' ;;
    *) return 1 ;;
  esac
}

gendev_transition_command() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G0:G1') printf '%s\n' 'scripts/init-project.sh' ;;
    'G1:G2') printf '%s\n' 'scripts/close-gate.sh G1' ;;
    'G2:G3') printf '%s\n' 'scripts/close-gate.sh G2' ;;
    'G3:G4') printf '%s\n' 'scripts/close-gate.sh G3' ;;
    'G4:G5') printf '%s\n' 'scripts/close-gate.sh G4' ;;
    'G5:G6') printf '%s\n' 'scripts/close-gate.sh G5' ;;
    'G6:G7') printf '%s\n' 'scripts/close-gate.sh G6' ;;
    'G7:G8') printf '%s\n' 'scripts/close-gate.sh G7' ;;
    'G8:G9') printf '%s\n' 'scripts/close-gate.sh G8' ;;
    *) return 1 ;;
  esac
}

gendev_transition_approval() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G0:G1') printf '%s\n' 'conditional_overwrite_only' ;;
    'G1:G2') printf '%s\n' 'named_human' ;;
    'G2:G3') printf '%s\n' 'named_human' ;;
    'G3:G4') printf '%s\n' 'named_human' ;;
    'G4:G5') printf '%s\n' 'named_human' ;;
    'G5:G6') printf '%s\n' 'conditional_nonwaivable_residual' ;;
    'G6:G7') printf '%s\n' 'conditional_skipped_critical_verification_or_risk' ;;
    'G7:G8') printf '%s\n' 'named_human_implementation_acceptance' ;;
    'G8:G9') printf '%s\n' 'named_human_project_closeout' ;;
    *) return 1 ;;
  esac
}

gendev_transition_required_artifacts() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G0:G1') printf '%s\n' 'project_manifest gate_log' ;;
    'G1:G2') printf '%s\n' 'vision' ;;
    'G2:G3') printf '%s\n' 'prd' ;;
    'G3:G4') printf '%s\n' 'architecture technology_stack' ;;
    'G4:G5') printf '%s\n' 'governance_security' ;;
    'G5:G6') printf '%s\n' 'phase_plan traceability' ;;
    'G6:G7') printf '%s\n' 'implementation_summary traceability' ;;
    'G7:G8') printf '%s\n' 'final_code_review aggregate_remediation final_test_uat traceability' ;;
    'G8:G9') printf '%s\n' 'deployment_record project_value_review traceability project_as_built' ;;
    *) return 1 ;;
  esac
}

gendev_transition_criteria() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G0:G1') printf '%s\n' 'G0-MANIFEST G0-SCAFFOLD G0-IDENTITY' ;;
    'G1:G2') printf '%s\n' 'G1-PROBLEM G1-USERS G1-SUCCESS G1-NON-GOALS G1-RISKS-QUESTIONS' ;;
    'G2:G3') printf '%s\n' 'G2-REQ-ID G2-AC-COVERAGE G2-EARS-FORM G2-OBSERVABLE-FORM G2-UNWANTED-BEHAVIOR G2-TESTABILITY' ;;
    'G3:G4') printf '%s\n' 'G3-ARCH-TRACE G3-BOUNDARIES G3-STATE-LIFECYCLE G3-STACK-ACCEPTED G3-VERIFICATION-SPEC G3-VERIFICATION-TRACE G3-DESIGN-INTERROGATION' ;;
    'G4:G5') printf '%s\n' 'G4-ACTOR-MODEL G4-AUTHORIZATION G4-AUDIT G4-SECRETS-DATA G4-TRUST-TOOLS G4-NEGATIVE-TESTS' ;;
    'G5:G6') printf '%s\n' 'G5-LOOP-AUTHORIZED G5-ALL-PHASES-EXITED G5-WHOLE-CANDIDATE G5-INTEGRATION-REGRESSION G5-CURRENT-TRACEABILITY G5-RESIDUAL-DISPOSITION' ;;
    'G6:G7') printf '%s\n' 'G6-IMPLEMENTATION-SUMMARY G6-CANDIDATE-REVISION G6-CHANGED-FILES G6-INTEGRATION-REGRESSION G6-DEVIATIONS-RESIDUALS G6-REVIEW-PACKAGE' ;;
    'G7:G8') printf '%s\n' 'G7-INDEPENDENT-REVIEW G7-REMEDIATION G7-FINAL-UAT G7-TRACEABILITY G7-RESIDUAL-RISK G7-UPSTREAM-BINDINGS' ;;
    'G8:G9') printf '%s\n' 'G9-DEPLOYMENT-DISPOSITION G9-OPERATIONS-RESULTS G9-VALUE-DISPOSITION G9-FINAL-TRACEABILITY G9-AS-BUILT-CLOSEOUT' ;;
    *) return 1 ;;
  esac
}

gendev_transition_required_event_bindings() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G0:G1') printf '%s\n' '' ;;
    'G1:G2') printf '%s\n' '' ;;
    'G2:G3') printf '%s\n' '' ;;
    'G3:G4') printf '%s\n' '' ;;
    'G4:G5') printf '%s\n' '' ;;
    'G5:G6') printf '%s\n' 'all_declared_phase_exit_event_ids' ;;
    'G6:G7') printf '%s\n' '' ;;
    'G7:G8') printf '%s\n' '' ;;
    'G8:G9') printf '%s\n' 'deployment_approval_or_non_deployment_approval' ;;
    *) return 1 ;;
  esac
}

gendev_transition_required_dynamic_evidence() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G0:G1') printf '%s\n' '' ;;
    'G1:G2') printf '%s\n' '' ;;
    'G2:G3') printf '%s\n' '' ;;
    'G3:G4') printf '%s\n' '' ;;
    'G4:G5') printf '%s\n' '' ;;
    'G5:G6') printf '%s\n' '' ;;
    'G6:G7') printf '%s\n' '' ;;
    'G7:G8') printf '%s\n' 'accepted_upstream_authority_set' ;;
    'G8:G9') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

gendev_transition_specific_event_fields() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G0:G1') printf '%s\n' '' ;;
    'G1:G2') printf '%s\n' '' ;;
    'G2:G3') printf '%s\n' '' ;;
    'G3:G4') printf '%s\n' '' ;;
    'G4:G5') printf '%s\n' '' ;;
    'G5:G6') printf '%s\n' 'whole_build_candidate_revision integration_regression_result residual_findings' ;;
    'G6:G7') printf '%s\n' '' ;;
    'G7:G8') printf '%s\n' 'accepted_release_candidate deployment_intent' ;;
    'G8:G9') printf '%s\n' 'deployment_disposition operational_results value_disposition terminal_closeout' ;;
    *) return 1 ;;
  esac
}

gendev_transition_conditional_event_profiles() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G0:G1') printf '%s\n' '' ;;
    'G1:G2') printf '%s\n' '' ;;
    'G2:G3') printf '%s\n' '' ;;
    'G3:G4') printf '%s\n' '' ;;
    'G4:G5') printf '%s\n' '' ;;
    'G5:G6') printf '%s\n' '' ;;
    'G6:G7') printf '%s\n' '' ;;
    'G7:G8') printf '%s\n' '' ;;
    'G8:G9') printf '%s\n' 'deploy' ;;
    *) return 1 ;;
  esac
}

gendev_transition_conditional_event_fields() {
  [ "$#" -eq 3 ] || return 2
  case "$1:$2:$3" in
    'G8:G9:deploy') printf '%s\n' 'operational_owner_confirmation' ;;
    *) return 1 ;;
  esac
}

gendev_transition_resulting_project_status() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G0:G1') printf '%s\n' 'initialized' ;;
    'G1:G2') printf '%s\n' 'active' ;;
    'G2:G3') printf '%s\n' 'active' ;;
    'G3:G4') printf '%s\n' 'active' ;;
    'G4:G5') printf '%s\n' 'active' ;;
    'G5:G6') printf '%s\n' 'active' ;;
    'G6:G7') printf '%s\n' 'active' ;;
    'G7:G8') printf '%s\n' 'active' ;;
    'G8:G9') printf '%s\n' 'closed' ;;
    *) return 1 ;;
  esac
}

gendev_transition_resulting_role() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G0:G1') printf '%s\n' 'product-vision-agent' ;;
    'G1:G2') printf '%s\n' 'prd-agent' ;;
    'G2:G3') printf '%s\n' 'architecture-agent' ;;
    'G3:G4') printf '%s\n' 'security-governance-agent' ;;
    'G4:G5') printf '%s\n' 'phase-planning-agent' ;;
    'G5:G6') printf '%s\n' 'implementation-agent' ;;
    'G6:G7') printf '%s\n' 'code-review-agent' ;;
    'G7:G8') printf '%s\n' 'deployment-readiness-agent' ;;
    'G8:G9') printf '%s\n' 'none' ;;
    *) return 1 ;;
  esac
}

gendev_transition_boolean() {
  [ "$#" -eq 3 ] || return 2
  case "$1:$2:$3" in
    'G0:G1:terminal') printf '%s\n' 'false' ;;
    'G1:G2:terminal') printf '%s\n' 'false' ;;
    'G2:G3:terminal') printf '%s\n' 'false' ;;
    'G3:G4:terminal') printf '%s\n' 'false' ;;
    'G4:G5:terminal') printf '%s\n' 'false' ;;
    'G5:G6:terminal') printf '%s\n' 'false' ;;
    'G6:G7:terminal') printf '%s\n' 'false' ;;
    'G7:G8:terminal') printf '%s\n' 'false' ;;
    'G8:G9:terminal') printf '%s\n' 'true' ;;
    'G8:G9:terminal_closeout') printf '%s\n' 'true' ;;
    *) return 1 ;;
  esac
}

gendev_transition_approval_profiles() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G0:G1') printf '%s\n' '' ;;
    'G1:G2') printf '%s\n' 'named_human' ;;
    'G2:G3') printf '%s\n' 'named_human' ;;
    'G3:G4') printf '%s\n' 'named_human' ;;
    'G4:G5') printf '%s\n' 'named_human' ;;
    'G5:G6') printf '%s\n' 'no_additional_approval named_human' ;;
    'G6:G7') printf '%s\n' 'no_additional_approval named_human' ;;
    'G7:G8') printf '%s\n' 'named_human' ;;
    'G8:G9') printf '%s\n' 'named_human' ;;
    *) return 1 ;;
  esac
}

gendev_transition_named_human_condition() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G5:G6') printf '%s\n' 'skipped_critical_verification_or_otherwise_nonwaivable_residual' ;;
    'G6:G7') printf '%s\n' 'skipped_critical_verification_or_allowed_risk_disposition' ;;
    *) return 1 ;;
  esac
}

gendev_transition_artifact_requirement_policy() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G0:G1') printf '%s\n' 'project_initialization_result' ;;
    'G1:G2') printf '%s\n' 'evidence_bindings' ;;
    'G2:G3') printf '%s\n' 'evidence_bindings' ;;
    'G3:G4') printf '%s\n' 'evidence_bindings' ;;
    'G4:G5') printf '%s\n' 'evidence_bindings' ;;
    'G5:G6') printf '%s\n' 'evidence_bindings' ;;
    'G6:G7') printf '%s\n' 'evidence_bindings' ;;
    'G7:G8') printf '%s\n' 'evidence_bindings' ;;
    'G8:G9') printf '%s\n' 'evidence_bindings' ;;
    *) return 1 ;;
  esac
}

gendev_transition_artifact_requirement_ids() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G0:G1') printf '%s\n' '' ;;
    'G1:G2') printf '%s\n' 'vision' ;;
    'G2:G3') printf '%s\n' 'prd' ;;
    'G3:G4') printf '%s\n' 'architecture technology_stack' ;;
    'G4:G5') printf '%s\n' 'governance_security' ;;
    'G5:G6') printf '%s\n' 'phase_plan traceability' ;;
    'G6:G7') printf '%s\n' 'implementation_summary traceability' ;;
    'G7:G8') printf '%s\n' 'final_code_review aggregate_remediation final_test_uat traceability' ;;
    'G8:G9') printf '%s\n' 'deployment_record project_value_review traceability project_as_built' ;;
    *) return 1 ;;
  esac
}

gendev_transition_artifact_reviewed_statuses() {
  [ "$#" -eq 3 ] || return 2
  case "$1:$2:$3" in
    'G1:G2:vision') printf '%s\n' 'Ready for Approval' ;;
    'G2:G3:prd') printf '%s\n' 'Ready for Approval' ;;
    'G3:G4:architecture') printf '%s\n' 'Ready for Approval' ;;
    'G3:G4:technology_stack') printf '%s\n' 'Ready for Approval' ;;
    'G4:G5:governance_security') printf '%s\n' 'Ready for Approval' ;;
    'G5:G6:phase_plan') printf '%s\n' 'Accepted' ;;
    'G5:G6:traceability') printf '%s\n' 'Complete' ;;
    'G6:G7:implementation_summary') printf '%s\n' 'Complete' ;;
    'G6:G7:traceability') printf '%s\n' 'Complete' ;;
    'G7:G8:final_code_review') printf '%s\n' 'Complete' ;;
    'G7:G8:aggregate_remediation') printf '%s\n' 'Complete' ;;
    'G7:G8:final_test_uat') printf '%s\n' 'Complete' ;;
    'G7:G8:traceability') printf '%s\n' 'Complete' ;;
    'G8:G9:deployment_record') printf '%s\n' 'Complete' ;;
    'G8:G9:project_value_review') printf '%s\n' 'Complete' ;;
    'G8:G9:traceability') printf '%s\n' 'Complete' ;;
    'G8:G9:project_as_built') printf '%s\n' 'Complete' ;;
    *) return 1 ;;
  esac
}

gendev_transition_artifact_resulting_statuses() {
  [ "$#" -eq 3 ] || return 2
  case "$1:$2:$3" in
    'G1:G2:vision') printf '%s\n' 'Accepted' ;;
    'G2:G3:prd') printf '%s\n' 'Accepted' ;;
    'G3:G4:architecture') printf '%s\n' 'Accepted' ;;
    'G3:G4:technology_stack') printf '%s\n' 'Accepted' ;;
    'G4:G5:governance_security') printf '%s\n' 'Accepted' ;;
    'G5:G6:phase_plan') printf '%s\n' 'Accepted' ;;
    'G5:G6:traceability') printf '%s\n' 'Complete' ;;
    'G6:G7:implementation_summary') printf '%s\n' 'Complete' ;;
    'G6:G7:traceability') printf '%s\n' 'Complete' ;;
    'G7:G8:final_code_review') printf '%s\n' 'Complete' ;;
    'G7:G8:aggregate_remediation') printf '%s\n' 'Complete' ;;
    'G7:G8:final_test_uat') printf '%s\n' 'Complete' ;;
    'G7:G8:traceability') printf '%s\n' 'Complete' ;;
    'G8:G9:deployment_record') printf '%s\n' 'Complete' ;;
    'G8:G9:project_value_review') printf '%s\n' 'Complete' ;;
    'G8:G9:traceability') printf '%s\n' 'Complete' ;;
    'G8:G9:project_as_built') printf '%s\n' 'Complete' ;;
    *) return 1 ;;
  esac
}

gendev_transition_artifact_evidence_categories() {
  [ "$#" -eq 3 ] || return 2
  case "$1:$2:$3" in
    'G1:G2:vision') printf '%s\n' 'new_acceptance_status_only' ;;
    'G2:G3:prd') printf '%s\n' 'new_acceptance_status_only' ;;
    'G3:G4:architecture') printf '%s\n' 'new_acceptance_status_only' ;;
    'G3:G4:technology_stack') printf '%s\n' 'new_acceptance_status_only' ;;
    'G4:G5:governance_security') printf '%s\n' 'new_acceptance_status_only' ;;
    'G5:G6:phase_plan') printf '%s\n' 'accepted_authority_unchanged' ;;
    'G5:G6:traceability') printf '%s\n' 'complete_report_unchanged' ;;
    'G6:G7:implementation_summary') printf '%s\n' 'complete_report_unchanged' ;;
    'G6:G7:traceability') printf '%s\n' 'complete_report_unchanged' ;;
    'G7:G8:final_code_review') printf '%s\n' 'complete_report_unchanged' ;;
    'G7:G8:aggregate_remediation') printf '%s\n' 'complete_report_unchanged' ;;
    'G7:G8:final_test_uat') printf '%s\n' 'complete_report_unchanged' ;;
    'G7:G8:traceability') printf '%s\n' 'complete_report_unchanged' ;;
    'G8:G9:deployment_record') printf '%s\n' 'complete_report_unchanged' ;;
    'G8:G9:project_value_review') printf '%s\n' 'complete_report_unchanged' ;;
    'G8:G9:traceability') printf '%s\n' 'complete_report_unchanged' ;;
    'G8:G9:project_as_built') printf '%s\n' 'complete_report_unchanged' ;;
    *) return 1 ;;
  esac
}

gendev_transition_artifact_required_dispositions() {
  [ "$#" -eq 3 ] || return 2
  case "$1:$2:$3" in
    'G1:G2:vision') printf '%s\n' '' ;;
    'G2:G3:prd') printf '%s\n' '' ;;
    'G3:G4:architecture') printf '%s\n' '' ;;
    'G3:G4:technology_stack') printf '%s\n' '' ;;
    'G4:G5:governance_security') printf '%s\n' '' ;;
    'G5:G6:phase_plan') printf '%s\n' '' ;;
    'G5:G6:traceability') printf '%s\n' '' ;;
    'G6:G7:implementation_summary') printf '%s\n' '' ;;
    'G6:G7:traceability') printf '%s\n' '' ;;
    'G7:G8:final_code_review') printf '%s\n' '' ;;
    'G7:G8:aggregate_remediation') printf '%s\n' 'complete not_required' ;;
    'G7:G8:final_test_uat') printf '%s\n' '' ;;
    'G7:G8:traceability') printf '%s\n' '' ;;
    'G8:G9:deployment_record') printf '%s\n' '' ;;
    'G8:G9:project_value_review') printf '%s\n' 'complete not_due not_applicable' ;;
    'G8:G9:traceability') printf '%s\n' '' ;;
    'G8:G9:project_as_built') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

gendev_transition_artifact_disposition_contract() {
  [ "$#" -eq 3 ] || return 2
  case "$1:$2:$3" in
    'G1:G2:vision') printf '%s\n' '' ;;
    'G2:G3:prd') printf '%s\n' '' ;;
    'G3:G4:architecture') printf '%s\n' '' ;;
    'G3:G4:technology_stack') printf '%s\n' '' ;;
    'G4:G5:governance_security') printf '%s\n' '' ;;
    'G5:G6:phase_plan') printf '%s\n' '' ;;
    'G5:G6:traceability') printf '%s\n' '' ;;
    'G6:G7:implementation_summary') printf '%s\n' '' ;;
    'G6:G7:traceability') printf '%s\n' '' ;;
    'G7:G8:final_code_review') printf '%s\n' '' ;;
    'G7:G8:aggregate_remediation') printf '%s\n' '' ;;
    'G7:G8:final_test_uat') printf '%s\n' '' ;;
    'G7:G8:traceability') printf '%s\n' '' ;;
    'G8:G9:deployment_record') printf '%s\n' '' ;;
    'G8:G9:project_value_review') printf '%s\n' 'value_review_contract' ;;
    'G8:G9:traceability') printf '%s\n' '' ;;
    'G8:G9:project_as_built') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_pattern() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G5.0') printf '%s\n' '^G5\.0$' ;;
    'G5.<id>.1') printf '%s\n' '^G5\.([A-Za-z0-9]+(-[A-Za-z0-9]+)*)\.1$' ;;
    'G5.<id>.2') printf '%s\n' '^G5\.([A-Za-z0-9]+(-[A-Za-z0-9]+)*)\.2$' ;;
    'G5.<id>.3') printf '%s\n' '^G5\.([A-Za-z0-9]+(-[A-Za-z0-9]+)*)\.3$' ;;
    'G5.<id>.4') printf '%s\n' '^G5\.([A-Za-z0-9]+(-[A-Za-z0-9]+)*)\.4$' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_event_type() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G5.0') printf '%s\n' 'phase_checkpoint' ;;
    'G5.<id>.1') printf '%s\n' 'phase_checkpoint' ;;
    'G5.<id>.2') printf '%s\n' 'phase_checkpoint' ;;
    'G5.<id>.3') printf '%s\n' 'phase_checkpoint' ;;
    'G5.<id>.4') printf '%s\n' 'phase_transition' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_approval() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G5.0') printf '%s\n' 'named_human' ;;
    'G5.<id>.1') printf '%s\n' 'named_human' ;;
    'G5.<id>.2') printf '%s\n' 'named_human' ;;
    'G5.<id>.3') printf '%s\n' 'named_human' ;;
    'G5.<id>.4') printf '%s\n' 'named_human_phase_exit' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_order() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G5.0') printf '%s\n' '0' ;;
    'G5.<id>.1') printf '%s\n' '1' ;;
    'G5.<id>.2') printf '%s\n' '2' ;;
    'G5.<id>.3') printf '%s\n' '3' ;;
    'G5.<id>.4') printf '%s\n' '4' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_active_major_gate() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G5.0') printf '%s\n' 'G5' ;;
    'G5.<id>.1') printf '%s\n' 'G5' ;;
    'G5.<id>.2') printf '%s\n' 'G5' ;;
    'G5.<id>.3') printf '%s\n' 'G5' ;;
    'G5.<id>.4') printf '%s\n' 'G5' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_resulting_role() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G5.0') printf '%s\n' 'phase-planning-agent' ;;
    'G5.<id>.1') printf '%s\n' 'phase-planning-agent' ;;
    'G5.<id>.2') printf '%s\n' 'phase-planning-agent' ;;
    'G5.<id>.3') printf '%s\n' 'implementation-agent' ;;
    'G5.<id>.4') printf '%s\n' 'phase-planning-agent' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_required_artifacts() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G5.0') printf '%s\n' 'phase_plan traceability' ;;
    'G5.<id>.1') printf '%s\n' 'phase_build_plan phase_test_uat' ;;
    'G5.<id>.2') printf '%s\n' 'tactical_plan' ;;
    'G5.<id>.3') printf '%s\n' 'construction_directive build_prompt phase_test_uat' ;;
    'G5.<id>.4') printf '%s\n' 'implementation_evidence phase_test_uat phase_code_review phase_remediation traceability phase_as_built phase_learnings phase_value_review' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_required_evidence_classes() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G5.0') printf '%s\n' 'phase_partition requirement_coverage integration_criteria coverage_policy' ;;
    'G5.<id>.1') printf '%s\n' 'phase_scope phase_exit_test_design' ;;
    'G5.<id>.2') printf '%s\n' 'stable_tasks dependencies paths tests verification rollback' ;;
    'G5.<id>.3') printf '%s\n' 'pinned_upstream_authority complete_construction_directive accepted_test_uat_plan' ;;
    'G5.<id>.4') printf '%s\n' 'implementation_candidate executed_test_uat independent_phase_review remediation_disposition cumulative_traceability phase_as_built_closeout phase_learnings regression coverage_result residual_risks amendments phase_value_disposition named_approval' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_criteria() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G5.0') printf '%s\n' 'G5-PHASE-ORDER G5-REQ-COVERAGE G5-INTEGRATION G5-PARTITION G5-COVERAGE-POLICY' ;;
    'G5.<id>.1') printf '%s\n' 'G5-PHASE-SCOPE G5-PHASE-EXIT-TEST-DESIGN' ;;
    'G5.<id>.2') printf '%s\n' 'G5-TACTICAL-TASKS G5-TACTICAL-DEPENDENCIES G5-TACTICAL-PATHS G5-TACTICAL-TESTS G5-TACTICAL-VERIFICATION G5-TACTICAL-ROLLBACK' ;;
    'G5.<id>.3') printf '%s\n' 'G5-DIRECTIVE-COMPLETE G5-BUILD-PROMPT G5-TEST-PLAN-ACCEPTED G5-UPSTREAM-PINNED G5-IMPLEMENTATION-APPROVAL' ;;
    'G5.<id>.4') printf '%s\n' 'G5-EXIT-IMPLEMENTATION G5-EXIT-TEST-UAT G5-EXIT-REVIEW G5-EXIT-REMEDIATION G5-EXIT-TRACEABILITY G5-EXIT-AS-BUILT G5-EXIT-LEARNINGS G5-EXIT-REGRESSION G5-EXIT-COVERAGE G5-EXIT-RESIDUALS G5-EXIT-AMENDMENTS G5-EXIT-VALUE-DISPOSITION G5-EXIT-APPROVAL' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_required_event_fields() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G5.0') printf '%s\n' 'event_id event_type schema_version project major_gate position phase_id criterion_ids evidence references decision approver approved_on checked_statement risk_disposition enforcement_context manifest_result next_state' ;;
    'G5.<id>.1') printf '%s\n' 'event_id event_type schema_version project major_gate position phase_id criterion_ids evidence references decision approver approved_on checked_statement risk_disposition enforcement_context manifest_result next_state' ;;
    'G5.<id>.2') printf '%s\n' 'event_id event_type schema_version project major_gate position phase_id criterion_ids evidence references decision approver approved_on checked_statement risk_disposition enforcement_context manifest_result next_state' ;;
    'G5.<id>.3') printf '%s\n' 'event_id event_type schema_version project major_gate position phase_id criterion_ids evidence references decision approver approved_on checked_statement risk_disposition enforcement_context manifest_result next_state' ;;
    'G5.<id>.4') printf '%s\n' 'event_id event_type schema_version project major_gate position phase_id candidate_revision criterion_ids evidence references test_uat_execution blocking_finding_count remediation_disposition phase_requirement_ids regression_result decision approver approved_on checked_statement risk_disposition coverage_result residual_findings amendments enforcement_context manifest_result next_state' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_nullable_event_fields() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G5.0') printf '%s\n' 'phase_id' ;;
    'G5.<id>.1') printf '%s\n' '' ;;
    'G5.<id>.2') printf '%s\n' '' ;;
    'G5.<id>.3') printf '%s\n' '' ;;
    'G5.<id>.4') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_required_dynamic_evidence() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G5.0') printf '%s\n' '' ;;
    'G5.<id>.1') printf '%s\n' '' ;;
    'G5.<id>.2') printf '%s\n' '' ;;
    'G5.<id>.3') printf '%s\n' 'accepted_upstream_authority_set' ;;
    'G5.<id>.4') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_reference_statuses_for_disposition() {
  [ "$#" -eq 3 ] || return 2
  case "$1:$2:$3" in
    'G5.<id>.4:phase_value_review:complete') printf '%s\n' 'Complete' ;;
    'G5.<id>.4:phase_value_review:not_due') printf '%s\n' 'Draft' ;;
    'G5.<id>.4:phase_value_review:not_applicable') printf '%s\n' 'Complete' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_artifact_requirement_ids() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G5.0') printf '%s\n' 'phase_plan traceability' ;;
    'G5.<id>.1') printf '%s\n' 'phase_build_plan' ;;
    'G5.<id>.2') printf '%s\n' 'tactical_plan' ;;
    'G5.<id>.3') printf '%s\n' 'construction_directive build_prompt phase_test_uat' ;;
    'G5.<id>.4') printf '%s\n' 'implementation_evidence phase_test_uat phase_code_review phase_remediation traceability phase_as_built phase_learnings' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_reference_artifacts() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'G5.0') printf '%s\n' '' ;;
    'G5.<id>.1') printf '%s\n' 'phase_test_uat' ;;
    'G5.<id>.2') printf '%s\n' '' ;;
    'G5.<id>.3') printf '%s\n' '' ;;
    'G5.<id>.4') printf '%s\n' 'phase_value_review' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_artifact_reviewed_statuses() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G5.0:phase_plan') printf '%s\n' 'Ready for Approval' ;;
    'G5.0:traceability') printf '%s\n' 'Complete' ;;
    'G5.<id>.1:phase_build_plan') printf '%s\n' 'Ready for Approval' ;;
    'G5.<id>.2:tactical_plan') printf '%s\n' 'Ready for Approval' ;;
    'G5.<id>.3:construction_directive') printf '%s\n' 'Ready for Approval' ;;
    'G5.<id>.3:build_prompt') printf '%s\n' 'Ready for Approval' ;;
    'G5.<id>.3:phase_test_uat') printf '%s\n' 'Ready for Approval' ;;
    'G5.<id>.4:implementation_evidence') printf '%s\n' 'Complete' ;;
    'G5.<id>.4:phase_test_uat') printf '%s\n' 'Accepted' ;;
    'G5.<id>.4:phase_code_review') printf '%s\n' 'Complete' ;;
    'G5.<id>.4:phase_remediation') printf '%s\n' 'Complete' ;;
    'G5.<id>.4:traceability') printf '%s\n' 'Complete' ;;
    'G5.<id>.4:phase_as_built') printf '%s\n' 'Complete' ;;
    'G5.<id>.4:phase_learnings') printf '%s\n' 'Ready for Approval' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_artifact_resulting_statuses() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G5.0:phase_plan') printf '%s\n' 'Accepted' ;;
    'G5.0:traceability') printf '%s\n' 'Complete' ;;
    'G5.<id>.1:phase_build_plan') printf '%s\n' 'Accepted' ;;
    'G5.<id>.2:tactical_plan') printf '%s\n' 'Accepted' ;;
    'G5.<id>.3:construction_directive') printf '%s\n' 'Accepted' ;;
    'G5.<id>.3:build_prompt') printf '%s\n' 'Accepted' ;;
    'G5.<id>.3:phase_test_uat') printf '%s\n' 'Accepted' ;;
    'G5.<id>.4:implementation_evidence') printf '%s\n' 'Complete' ;;
    'G5.<id>.4:phase_test_uat') printf '%s\n' 'Accepted' ;;
    'G5.<id>.4:phase_code_review') printf '%s\n' 'Complete' ;;
    'G5.<id>.4:phase_remediation') printf '%s\n' 'Complete' ;;
    'G5.<id>.4:traceability') printf '%s\n' 'Complete' ;;
    'G5.<id>.4:phase_as_built') printf '%s\n' 'Complete' ;;
    'G5.<id>.4:phase_learnings') printf '%s\n' 'Accepted' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_artifact_evidence_categories() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G5.0:phase_plan') printf '%s\n' 'new_acceptance_status_only' ;;
    'G5.0:traceability') printf '%s\n' 'complete_report_unchanged' ;;
    'G5.<id>.1:phase_build_plan') printf '%s\n' 'new_acceptance_status_only' ;;
    'G5.<id>.2:tactical_plan') printf '%s\n' 'new_acceptance_status_only' ;;
    'G5.<id>.3:construction_directive') printf '%s\n' 'new_acceptance_status_only' ;;
    'G5.<id>.3:build_prompt') printf '%s\n' 'new_acceptance_status_only' ;;
    'G5.<id>.3:phase_test_uat') printf '%s\n' 'new_acceptance_status_only' ;;
    'G5.<id>.4:implementation_evidence') printf '%s\n' 'complete_report_unchanged' ;;
    'G5.<id>.4:phase_test_uat') printf '%s\n' 'accepted_authority_unchanged' ;;
    'G5.<id>.4:phase_code_review') printf '%s\n' 'complete_report_unchanged' ;;
    'G5.<id>.4:phase_remediation') printf '%s\n' 'complete_report_unchanged' ;;
    'G5.<id>.4:traceability') printf '%s\n' 'complete_report_unchanged' ;;
    'G5.<id>.4:phase_as_built') printf '%s\n' 'complete_report_unchanged' ;;
    'G5.<id>.4:phase_learnings') printf '%s\n' 'new_acceptance_status_only' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_artifact_required_dispositions() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G5.0:phase_plan') printf '%s\n' '' ;;
    'G5.0:traceability') printf '%s\n' '' ;;
    'G5.<id>.1:phase_build_plan') printf '%s\n' '' ;;
    'G5.<id>.2:tactical_plan') printf '%s\n' '' ;;
    'G5.<id>.3:construction_directive') printf '%s\n' '' ;;
    'G5.<id>.3:build_prompt') printf '%s\n' '' ;;
    'G5.<id>.3:phase_test_uat') printf '%s\n' '' ;;
    'G5.<id>.4:implementation_evidence') printf '%s\n' '' ;;
    'G5.<id>.4:phase_test_uat') printf '%s\n' '' ;;
    'G5.<id>.4:phase_code_review') printf '%s\n' '' ;;
    'G5.<id>.4:phase_remediation') printf '%s\n' 'complete not_required' ;;
    'G5.<id>.4:traceability') printf '%s\n' '' ;;
    'G5.<id>.4:phase_as_built') printf '%s\n' '' ;;
    'G5.<id>.4:phase_learnings') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_artifact_disposition_contract() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G5.0:phase_plan') printf '%s\n' '' ;;
    'G5.0:traceability') printf '%s\n' '' ;;
    'G5.<id>.1:phase_build_plan') printf '%s\n' '' ;;
    'G5.<id>.2:tactical_plan') printf '%s\n' '' ;;
    'G5.<id>.3:construction_directive') printf '%s\n' '' ;;
    'G5.<id>.3:build_prompt') printf '%s\n' '' ;;
    'G5.<id>.3:phase_test_uat') printf '%s\n' '' ;;
    'G5.<id>.4:implementation_evidence') printf '%s\n' '' ;;
    'G5.<id>.4:phase_test_uat') printf '%s\n' '' ;;
    'G5.<id>.4:phase_code_review') printf '%s\n' '' ;;
    'G5.<id>.4:phase_remediation') printf '%s\n' '' ;;
    'G5.<id>.4:traceability') printf '%s\n' '' ;;
    'G5.<id>.4:phase_as_built') printf '%s\n' '' ;;
    'G5.<id>.4:phase_learnings') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_reference_allowed_statuses() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G5.<id>.1:phase_test_uat') printf '%s\n' 'Draft Ready for Review Ready for Approval Accepted' ;;
    'G5.<id>.4:phase_value_review') printf '%s\n' 'Draft Complete' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_reference_required_dispositions() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G5.<id>.1:phase_test_uat') printf '%s\n' '' ;;
    'G5.<id>.4:phase_value_review') printf '%s\n' 'complete not_due not_applicable' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_reference_required_fields() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G5.<id>.1:phase_test_uat') printf '%s\n' 'artifact_path status revision blob_oid digest' ;;
    'G5.<id>.4:phase_value_review') printf '%s\n' 'artifact_path status revision blob_oid digest value_review.disposition value_review.details' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_reference_binding_mode() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G5.<id>.1:phase_test_uat') printf '%s\n' 'pinned_checkpoint_reference' ;;
    'G5.<id>.4:phase_value_review') printf '%s\n' 'pinned_subordinate_reference' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_reference_parent_artifact() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G5.<id>.1:phase_test_uat') printf '%s\n' '' ;;
    'G5.<id>.4:phase_value_review') printf '%s\n' 'phase_as_built' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_reference_disposition_contract() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G5.<id>.1:phase_test_uat') printf '%s\n' '' ;;
    'G5.<id>.4:phase_value_review') printf '%s\n' 'value_review_contract' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_evidence_artifacts() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G5.0:phase_partition') printf '%s\n' 'phase_plan' ;;
    'G5.0:requirement_coverage') printf '%s\n' 'phase_plan traceability' ;;
    'G5.0:integration_criteria') printf '%s\n' 'phase_plan' ;;
    'G5.0:coverage_policy') printf '%s\n' 'phase_plan' ;;
    'G5.<id>.1:phase_scope') printf '%s\n' 'phase_build_plan' ;;
    'G5.<id>.1:phase_exit_test_design') printf '%s\n' 'phase_build_plan' ;;
    'G5.<id>.2:stable_tasks') printf '%s\n' 'tactical_plan' ;;
    'G5.<id>.2:dependencies') printf '%s\n' 'tactical_plan' ;;
    'G5.<id>.2:paths') printf '%s\n' 'tactical_plan' ;;
    'G5.<id>.2:tests') printf '%s\n' 'tactical_plan' ;;
    'G5.<id>.2:verification') printf '%s\n' 'tactical_plan' ;;
    'G5.<id>.2:rollback') printf '%s\n' 'tactical_plan' ;;
    'G5.<id>.3:pinned_upstream_authority') printf '%s\n' 'construction_directive' ;;
    'G5.<id>.3:complete_construction_directive') printf '%s\n' 'construction_directive' ;;
    'G5.<id>.3:accepted_test_uat_plan') printf '%s\n' 'phase_test_uat' ;;
    'G5.<id>.4:implementation_candidate') printf '%s\n' 'implementation_evidence' ;;
    'G5.<id>.4:executed_test_uat') printf '%s\n' 'phase_test_uat implementation_evidence' ;;
    'G5.<id>.4:independent_phase_review') printf '%s\n' 'phase_code_review' ;;
    'G5.<id>.4:remediation_disposition') printf '%s\n' 'phase_remediation' ;;
    'G5.<id>.4:cumulative_traceability') printf '%s\n' 'traceability' ;;
    'G5.<id>.4:phase_as_built_closeout') printf '%s\n' 'phase_as_built' ;;
    'G5.<id>.4:phase_learnings') printf '%s\n' 'phase_learnings' ;;
    'G5.<id>.4:regression') printf '%s\n' 'implementation_evidence phase_test_uat' ;;
    'G5.<id>.4:coverage_result') printf '%s\n' 'implementation_evidence phase_test_uat' ;;
    'G5.<id>.4:residual_risks') printf '%s\n' 'phase_code_review phase_remediation' ;;
    'G5.<id>.4:amendments') printf '%s\n' '' ;;
    'G5.<id>.4:phase_value_disposition') printf '%s\n' 'phase_as_built' ;;
    'G5.<id>.4:named_approval') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_evidence_event_fields() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G5.0:phase_partition') printf '%s\n' 'evidence' ;;
    'G5.0:requirement_coverage') printf '%s\n' 'evidence' ;;
    'G5.0:integration_criteria') printf '%s\n' 'evidence' ;;
    'G5.0:coverage_policy') printf '%s\n' 'evidence' ;;
    'G5.<id>.1:phase_scope') printf '%s\n' 'evidence' ;;
    'G5.<id>.1:phase_exit_test_design') printf '%s\n' 'evidence references' ;;
    'G5.<id>.2:stable_tasks') printf '%s\n' 'evidence' ;;
    'G5.<id>.2:dependencies') printf '%s\n' 'evidence' ;;
    'G5.<id>.2:paths') printf '%s\n' 'evidence' ;;
    'G5.<id>.2:tests') printf '%s\n' 'evidence' ;;
    'G5.<id>.2:verification') printf '%s\n' 'evidence' ;;
    'G5.<id>.2:rollback') printf '%s\n' 'evidence' ;;
    'G5.<id>.3:pinned_upstream_authority') printf '%s\n' 'evidence' ;;
    'G5.<id>.3:complete_construction_directive') printf '%s\n' 'evidence' ;;
    'G5.<id>.3:accepted_test_uat_plan') printf '%s\n' 'evidence' ;;
    'G5.<id>.4:implementation_candidate') printf '%s\n' 'candidate_revision evidence' ;;
    'G5.<id>.4:executed_test_uat') printf '%s\n' 'evidence test_uat_execution' ;;
    'G5.<id>.4:independent_phase_review') printf '%s\n' 'evidence blocking_finding_count' ;;
    'G5.<id>.4:remediation_disposition') printf '%s\n' 'evidence remediation_disposition' ;;
    'G5.<id>.4:cumulative_traceability') printf '%s\n' 'evidence phase_requirement_ids' ;;
    'G5.<id>.4:phase_as_built_closeout') printf '%s\n' 'evidence' ;;
    'G5.<id>.4:phase_learnings') printf '%s\n' 'evidence' ;;
    'G5.<id>.4:regression') printf '%s\n' 'regression_result' ;;
    'G5.<id>.4:coverage_result') printf '%s\n' 'coverage_result' ;;
    'G5.<id>.4:residual_risks') printf '%s\n' 'residual_findings' ;;
    'G5.<id>.4:amendments') printf '%s\n' 'amendments' ;;
    'G5.<id>.4:phase_value_disposition') printf '%s\n' 'evidence references' ;;
    'G5.<id>.4:named_approval') printf '%s\n' 'approver approved_on checked_statement risk_disposition' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_evidence_referenced_artifacts() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G5.0:phase_partition') printf '%s\n' '' ;;
    'G5.0:requirement_coverage') printf '%s\n' '' ;;
    'G5.0:integration_criteria') printf '%s\n' '' ;;
    'G5.0:coverage_policy') printf '%s\n' '' ;;
    'G5.<id>.1:phase_scope') printf '%s\n' '' ;;
    'G5.<id>.1:phase_exit_test_design') printf '%s\n' 'phase_test_uat' ;;
    'G5.<id>.2:stable_tasks') printf '%s\n' '' ;;
    'G5.<id>.2:dependencies') printf '%s\n' '' ;;
    'G5.<id>.2:paths') printf '%s\n' '' ;;
    'G5.<id>.2:tests') printf '%s\n' '' ;;
    'G5.<id>.2:verification') printf '%s\n' '' ;;
    'G5.<id>.2:rollback') printf '%s\n' '' ;;
    'G5.<id>.3:pinned_upstream_authority') printf '%s\n' '' ;;
    'G5.<id>.3:complete_construction_directive') printf '%s\n' '' ;;
    'G5.<id>.3:accepted_test_uat_plan') printf '%s\n' '' ;;
    'G5.<id>.4:implementation_candidate') printf '%s\n' '' ;;
    'G5.<id>.4:executed_test_uat') printf '%s\n' '' ;;
    'G5.<id>.4:independent_phase_review') printf '%s\n' '' ;;
    'G5.<id>.4:remediation_disposition') printf '%s\n' '' ;;
    'G5.<id>.4:cumulative_traceability') printf '%s\n' '' ;;
    'G5.<id>.4:phase_as_built_closeout') printf '%s\n' '' ;;
    'G5.<id>.4:phase_learnings') printf '%s\n' '' ;;
    'G5.<id>.4:regression') printf '%s\n' '' ;;
    'G5.<id>.4:coverage_result') printf '%s\n' '' ;;
    'G5.<id>.4:residual_risks') printf '%s\n' '' ;;
    'G5.<id>.4:amendments') printf '%s\n' '' ;;
    'G5.<id>.4:phase_value_disposition') printf '%s\n' 'phase_value_review' ;;
    'G5.<id>.4:named_approval') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

gendev_checkpoint_evidence_binding_mode() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'G5.0:phase_partition') printf '%s\n' 'direct_evidence' ;;
    'G5.0:requirement_coverage') printf '%s\n' 'direct_evidence' ;;
    'G5.0:integration_criteria') printf '%s\n' 'direct_evidence' ;;
    'G5.0:coverage_policy') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.1:phase_scope') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.1:phase_exit_test_design') printf '%s\n' 'pinned_checkpoint_reference' ;;
    'G5.<id>.2:stable_tasks') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.2:dependencies') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.2:paths') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.2:tests') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.2:verification') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.2:rollback') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.3:pinned_upstream_authority') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.3:complete_construction_directive') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.3:accepted_test_uat_plan') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.4:implementation_candidate') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.4:executed_test_uat') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.4:independent_phase_review') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.4:remediation_disposition') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.4:cumulative_traceability') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.4:phase_as_built_closeout') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.4:phase_learnings') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.4:regression') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.4:coverage_result') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.4:residual_risks') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.4:amendments') printf '%s\n' 'direct_evidence' ;;
    'G5.<id>.4:phase_value_disposition') printf '%s\n' 'pinned_subordinate_reference' ;;
    'G5.<id>.4:named_approval') printf '%s\n' 'direct_evidence' ;;
    *) return 1 ;;
  esac
}

gendev_artifact_path() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'project_manifest') printf '%s\n' 'docs/project/project.yaml' ;;
    'gate_log') printf '%s\n' 'docs/project/approvals/gate-log.md' ;;
    'vision') printf '%s\n' 'docs/project/vision/vision.md' ;;
    'prd') printf '%s\n' 'docs/project/prd/prd.md' ;;
    'architecture') printf '%s\n' 'docs/project/architecture/architecture.md' ;;
    'technology_stack') printf '%s\n' 'docs/project/decisions/0001-technology-stack.md' ;;
    'governance_security') printf '%s\n' 'docs/project/security-governance/governance-security-spec.md' ;;
    'phase_plan') printf '%s\n' 'docs/project/build-plan/phase-plan.md' ;;
    'phase_build_plan') printf '%s\n' 'docs/project/build-plan/phases/phase-<id>-build-plan.md' ;;
    'tactical_plan') printf '%s\n' 'docs/project/build-plan/phases/phase-<id>-tactical-implementation-plan.md' ;;
    'construction_directive') printf '%s\n' 'docs/project/build-plan/phases/phase-<id>-construction-directive.md' ;;
    'build_prompt') printf '%s\n' 'docs/project/build-plan/phases/phase-<id>-build-prompt.md' ;;
    'phase_test_uat') printf '%s\n' 'docs/project/testing/phase-<id>-test-uat-plan.md' ;;
    'implementation_evidence') printf '%s\n' 'docs/project/build-plan/phases/phase-<id>-implementation-evidence.md' ;;
    'phase_code_review') printf '%s\n' 'docs/project/build-plan/phases/phase-<id>-code-review.md' ;;
    'phase_remediation') printf '%s\n' 'docs/project/build-plan/phases/phase-<id>-remediation.md' ;;
    'phase_learnings') printf '%s\n' 'docs/project/build-plan/phases/phase-<id>-learnings.md' ;;
    'phase_as_built') printf '%s\n' 'docs/project/as-built/phase-<id>-as-built-closeout.md' ;;
    'phase_value_review') printf '%s\n' 'docs/project/as-built/phase-<id>-value-review.md' ;;
    'traceability') printf '%s\n' 'docs/project/traceability/traceability-matrix.md' ;;
    'implementation_summary') printf '%s\n' 'docs/project/build-plan/implementation-summary.md' ;;
    'final_code_review') printf '%s\n' 'docs/project/review/code-review.md' ;;
    'aggregate_remediation') printf '%s\n' 'docs/project/review/remediation.md' ;;
    'final_test_uat') printf '%s\n' 'docs/project/testing/final-test-uat-report.md' ;;
    'deployment_readiness') printf '%s\n' 'docs/project/deployment/deployment-readiness.md' ;;
    'production_runbook') printf '%s\n' 'docs/project/deployment/production-runbook.md' ;;
    'deployment_record') printf '%s\n' 'docs/project/deployment/deployment-record.md' ;;
    'project_value_review') printf '%s\n' 'docs/project/as-built/value-review.md' ;;
    'project_as_built') printf '%s\n' 'docs/project/as-built/as-built-closeout.md' ;;
    'supporting_design') printf '%s\n' 'docs/project/design/<name>.md' ;;
    *) return 1 ;;
  esac
}

gendev_artifact_path_kind() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'project_manifest') printf '%s\n' 'fixed' ;;
    'gate_log') printf '%s\n' 'fixed' ;;
    'vision') printf '%s\n' 'fixed' ;;
    'prd') printf '%s\n' 'fixed' ;;
    'architecture') printf '%s\n' 'fixed' ;;
    'technology_stack') printf '%s\n' 'fixed' ;;
    'governance_security') printf '%s\n' 'fixed' ;;
    'phase_plan') printf '%s\n' 'fixed' ;;
    'phase_build_plan') printf '%s\n' 'phase_pattern' ;;
    'tactical_plan') printf '%s\n' 'phase_pattern' ;;
    'construction_directive') printf '%s\n' 'phase_pattern' ;;
    'build_prompt') printf '%s\n' 'phase_pattern' ;;
    'phase_test_uat') printf '%s\n' 'phase_pattern' ;;
    'implementation_evidence') printf '%s\n' 'phase_pattern' ;;
    'phase_code_review') printf '%s\n' 'phase_pattern' ;;
    'phase_remediation') printf '%s\n' 'phase_pattern' ;;
    'phase_learnings') printf '%s\n' 'phase_pattern' ;;
    'phase_as_built') printf '%s\n' 'phase_pattern' ;;
    'phase_value_review') printf '%s\n' 'phase_pattern' ;;
    'traceability') printf '%s\n' 'fixed' ;;
    'implementation_summary') printf '%s\n' 'fixed' ;;
    'final_code_review') printf '%s\n' 'fixed' ;;
    'aggregate_remediation') printf '%s\n' 'fixed' ;;
    'final_test_uat') printf '%s\n' 'fixed' ;;
    'deployment_readiness') printf '%s\n' 'fixed' ;;
    'production_runbook') printf '%s\n' 'fixed' ;;
    'deployment_record') printf '%s\n' 'fixed' ;;
    'project_value_review') printf '%s\n' 'fixed' ;;
    'project_as_built') printf '%s\n' 'fixed' ;;
    'supporting_design') printf '%s\n' 'supporting_pattern' ;;
    *) return 1 ;;
  esac
}

gendev_artifact_kind() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'project_manifest') printf '%s\n' 'control_plane' ;;
    'gate_log') printf '%s\n' 'append_only_event_log' ;;
    'vision') printf '%s\n' 'authority' ;;
    'prd') printf '%s\n' 'authority' ;;
    'architecture') printf '%s\n' 'authority' ;;
    'technology_stack') printf '%s\n' 'authority' ;;
    'governance_security') printf '%s\n' 'authority' ;;
    'phase_plan') printf '%s\n' 'authority' ;;
    'phase_build_plan') printf '%s\n' 'authority' ;;
    'tactical_plan') printf '%s\n' 'authority' ;;
    'construction_directive') printf '%s\n' 'authority' ;;
    'build_prompt') printf '%s\n' 'execution_instruction' ;;
    'phase_test_uat') printf '%s\n' 'authority_and_evidence' ;;
    'implementation_evidence') printf '%s\n' 'evidence_report' ;;
    'phase_code_review') printf '%s\n' 'evidence_report' ;;
    'phase_remediation') printf '%s\n' 'evidence_report' ;;
    'phase_learnings') printf '%s\n' 'authority' ;;
    'phase_as_built') printf '%s\n' 'evidence_report' ;;
    'phase_value_review') printf '%s\n' 'value_evidence' ;;
    'traceability') printf '%s\n' 'cumulative_evidence' ;;
    'implementation_summary') printf '%s\n' 'evidence_report' ;;
    'final_code_review') printf '%s\n' 'evidence_report' ;;
    'aggregate_remediation') printf '%s\n' 'evidence_report' ;;
    'final_test_uat') printf '%s\n' 'evidence_report' ;;
    'deployment_readiness') printf '%s\n' 'authority' ;;
    'production_runbook') printf '%s\n' 'evidence_report' ;;
    'deployment_record') printf '%s\n' 'evidence_report' ;;
    'project_value_review') printf '%s\n' 'value_evidence' ;;
    'project_as_built') printf '%s\n' 'evidence_report' ;;
    'supporting_design') printf '%s\n' 'supporting_design' ;;
    *) return 1 ;;
  esac
}

gendev_artifact_template() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'project_manifest') printf '%s\n' 'docs/project-template/project.yaml' ;;
    'gate_log') printf '%s\n' 'docs/methodology/templates/gate-log-template.md' ;;
    'vision') printf '%s\n' 'docs/methodology/templates/vision-template.md' ;;
    'prd') printf '%s\n' 'docs/methodology/templates/prd-template.md' ;;
    'architecture') printf '%s\n' 'docs/methodology/templates/architecture-template.md' ;;
    'technology_stack') printf '%s\n' 'docs/methodology/templates/0001-technology-stack-template.md' ;;
    'governance_security') printf '%s\n' 'docs/methodology/templates/governance-security-template.md' ;;
    'phase_plan') printf '%s\n' 'docs/methodology/templates/phase-plan-template.md' ;;
    'phase_build_plan') printf '%s\n' 'docs/methodology/templates/phase-build-plan-template.md' ;;
    'tactical_plan') printf '%s\n' 'docs/methodology/templates/tactical-implementation-template.md' ;;
    'construction_directive') printf '%s\n' 'docs/methodology/templates/phase-construction-directive-template.md' ;;
    'build_prompt') printf '%s\n' 'docs/methodology/templates/phase-build-prompt-template.md' ;;
    'phase_test_uat') printf '%s\n' 'docs/methodology/templates/test-uat-plan-template.md' ;;
    'implementation_evidence') printf '%s\n' 'docs/methodology/templates/phase-implementation-evidence-template.md' ;;
    'phase_code_review') printf '%s\n' 'docs/methodology/templates/code-review-report-template.md' ;;
    'phase_remediation') printf '%s\n' 'docs/methodology/templates/phase-remediation-template.md' ;;
    'phase_learnings') printf '%s\n' 'docs/methodology/templates/phase-learnings-template.md' ;;
    'phase_as_built') printf '%s\n' 'docs/methodology/templates/as-built-closeout-template.md' ;;
    'phase_value_review') printf '%s\n' 'docs/methodology/templates/value-review-template.md' ;;
    'traceability') printf '%s\n' 'docs/methodology/templates/traceability-matrix-template.md' ;;
    'implementation_summary') printf '%s\n' 'docs/methodology/templates/implementation-summary-template.md' ;;
    'final_code_review') printf '%s\n' 'docs/methodology/templates/final-code-review-report-template.md' ;;
    'aggregate_remediation') printf '%s\n' 'docs/methodology/templates/aggregate-remediation-template.md' ;;
    'final_test_uat') printf '%s\n' 'docs/methodology/templates/final-test-uat-report-template.md' ;;
    'deployment_readiness') printf '%s\n' 'docs/methodology/templates/deployment-readiness-template.md' ;;
    'production_runbook') printf '%s\n' 'docs/methodology/templates/production-runbook-template.md' ;;
    'deployment_record') printf '%s\n' 'docs/methodology/templates/deployment-record-template.md' ;;
    'project_value_review') printf '%s\n' 'docs/methodology/templates/project-value-review-template.md' ;;
    'project_as_built') printf '%s\n' 'docs/methodology/templates/project-as-built-closeout-template.md' ;;
    'supporting_design') printf '%s\n' 'docs/methodology/templates/adr-template.md' ;;
    *) return 1 ;;
  esac
}

gendev_artifact_template_state() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'project_manifest') printf '%s\n' 'current' ;;
    'gate_log') printf '%s\n' 'current' ;;
    'vision') printf '%s\n' 'current' ;;
    'prd') printf '%s\n' 'current' ;;
    'architecture') printf '%s\n' 'current' ;;
    'technology_stack') printf '%s\n' 'current' ;;
    'governance_security') printf '%s\n' 'current' ;;
    'phase_plan') printf '%s\n' 'current' ;;
    'phase_build_plan') printf '%s\n' 'current' ;;
    'tactical_plan') printf '%s\n' 'current' ;;
    'construction_directive') printf '%s\n' 'current' ;;
    'build_prompt') printf '%s\n' 'current' ;;
    'phase_test_uat') printf '%s\n' 'current' ;;
    'implementation_evidence') printf '%s\n' 'planned' ;;
    'phase_code_review') printf '%s\n' 'current' ;;
    'phase_remediation') printf '%s\n' 'planned' ;;
    'phase_learnings') printf '%s\n' 'current' ;;
    'phase_as_built') printf '%s\n' 'current' ;;
    'phase_value_review') printf '%s\n' 'current' ;;
    'traceability') printf '%s\n' 'current' ;;
    'implementation_summary') printf '%s\n' 'planned' ;;
    'final_code_review') printf '%s\n' 'planned' ;;
    'aggregate_remediation') printf '%s\n' 'planned' ;;
    'final_test_uat') printf '%s\n' 'planned' ;;
    'deployment_readiness') printf '%s\n' 'planned' ;;
    'production_runbook') printf '%s\n' 'planned' ;;
    'deployment_record') printf '%s\n' 'planned' ;;
    'project_value_review') printf '%s\n' 'planned' ;;
    'project_as_built') printf '%s\n' 'planned' ;;
    'supporting_design') printf '%s\n' 'current' ;;
    *) return 1 ;;
  esac
}

gendev_artifact_lifecycle_state() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'project_manifest') printf '%s\n' 'current' ;;
    'gate_log') printf '%s\n' 'current' ;;
    'vision') printf '%s\n' 'current' ;;
    'prd') printf '%s\n' 'current' ;;
    'architecture') printf '%s\n' 'current' ;;
    'technology_stack') printf '%s\n' 'current' ;;
    'governance_security') printf '%s\n' 'current' ;;
    'phase_plan') printf '%s\n' 'current' ;;
    'phase_build_plan') printf '%s\n' 'current' ;;
    'tactical_plan') printf '%s\n' 'current' ;;
    'construction_directive') printf '%s\n' 'current' ;;
    'build_prompt') printf '%s\n' 'current' ;;
    'phase_test_uat') printf '%s\n' 'current' ;;
    'implementation_evidence') printf '%s\n' 'planned' ;;
    'phase_code_review') printf '%s\n' 'current' ;;
    'phase_remediation') printf '%s\n' 'planned' ;;
    'phase_learnings') printf '%s\n' 'current' ;;
    'phase_as_built') printf '%s\n' 'current' ;;
    'phase_value_review') printf '%s\n' 'current' ;;
    'traceability') printf '%s\n' 'current' ;;
    'implementation_summary') printf '%s\n' 'planned' ;;
    'final_code_review') printf '%s\n' 'planned' ;;
    'aggregate_remediation') printf '%s\n' 'planned' ;;
    'final_test_uat') printf '%s\n' 'planned' ;;
    'deployment_readiness') printf '%s\n' 'planned' ;;
    'production_runbook') printf '%s\n' 'planned' ;;
    'deployment_record') printf '%s\n' 'planned' ;;
    'project_value_review') printf '%s\n' 'planned' ;;
    'project_as_built') printf '%s\n' 'planned' ;;
    'supporting_design') printf '%s\n' 'current' ;;
    *) return 1 ;;
  esac
}

gendev_artifact_evidence_class() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'project_manifest') printf '%s\n' 'manifest_state' ;;
    'gate_log') printf '%s\n' 'durable_approval_events' ;;
    'vision') printf '%s\n' 'accepted_authority' ;;
    'prd') printf '%s\n' 'accepted_authority' ;;
    'architecture') printf '%s\n' 'accepted_authority' ;;
    'technology_stack') printf '%s\n' 'accepted_authority' ;;
    'governance_security') printf '%s\n' 'accepted_authority' ;;
    'phase_plan') printf '%s\n' 'phase_partition' ;;
    'phase_build_plan') printf '%s\n' 'phase_scope' ;;
    'tactical_plan') printf '%s\n' 'stable_tasks' ;;
    'construction_directive') printf '%s\n' 'complete_construction_directive' ;;
    'build_prompt') printf '%s\n' 'build_prompt' ;;
    'phase_test_uat') printf '%s\n' 'executed_test_uat' ;;
    'implementation_evidence') printf '%s\n' 'implementation_candidate' ;;
    'phase_code_review') printf '%s\n' 'independent_phase_review' ;;
    'phase_remediation') printf '%s\n' 'remediation_disposition' ;;
    'phase_learnings') printf '%s\n' 'phase_learnings' ;;
    'phase_as_built') printf '%s\n' 'phase_as_built_closeout' ;;
    'phase_value_review') printf '%s\n' 'phase_value_disposition' ;;
    'traceability') printf '%s\n' 'cumulative_traceability' ;;
    'implementation_summary') printf '%s\n' 'aggregate_implementation_readiness' ;;
    'final_code_review') printf '%s\n' 'aggregate_independent_review' ;;
    'aggregate_remediation') printf '%s\n' 'aggregate_remediation' ;;
    'final_test_uat') printf '%s\n' 'aggregate_test_uat' ;;
    'deployment_readiness') printf '%s\n' 'deployment_readiness' ;;
    'production_runbook') printf '%s\n' 'production_operations' ;;
    'deployment_record') printf '%s\n' 'deployment_or_non_deployment_result' ;;
    'project_value_review') printf '%s\n' 'project_value_disposition' ;;
    'project_as_built') printf '%s\n' 'project_as_built_closeout' ;;
    'supporting_design') printf '%s\n' 'technique_specific_support' ;;
    *) return 1 ;;
  esac
}

gendev_artifact_owner_role() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'project_manifest') printf '%s\n' 'phase-planning-agent' ;;
    'gate_log') printf '%s\n' 'named-human-approver' ;;
    'vision') printf '%s\n' 'product-vision-agent' ;;
    'prd') printf '%s\n' 'prd-agent' ;;
    'architecture') printf '%s\n' 'architecture-agent' ;;
    'technology_stack') printf '%s\n' 'architecture-agent' ;;
    'governance_security') printf '%s\n' 'security-governance-agent' ;;
    'phase_plan') printf '%s\n' 'phase-planning-agent' ;;
    'phase_build_plan') printf '%s\n' 'phase-planning-agent' ;;
    'tactical_plan') printf '%s\n' 'phase-planning-agent' ;;
    'construction_directive') printf '%s\n' 'phase-planning-agent' ;;
    'build_prompt') printf '%s\n' 'phase-planning-agent' ;;
    'phase_test_uat') printf '%s\n' 'test-uat-agent' ;;
    'implementation_evidence') printf '%s\n' 'implementation-agent' ;;
    'phase_code_review') printf '%s\n' 'code-review-agent' ;;
    'phase_remediation') printf '%s\n' 'remediation-agent' ;;
    'phase_learnings') printf '%s\n' 'as-built-closeout-agent' ;;
    'phase_as_built') printf '%s\n' 'as-built-closeout-agent' ;;
    'phase_value_review') printf '%s\n' 'as-built-closeout-agent' ;;
    'traceability') printf '%s\n' 'test-uat-agent' ;;
    'implementation_summary') printf '%s\n' 'implementation-agent' ;;
    'final_code_review') printf '%s\n' 'code-review-agent' ;;
    'aggregate_remediation') printf '%s\n' 'remediation-agent' ;;
    'final_test_uat') printf '%s\n' 'test-uat-agent' ;;
    'deployment_readiness') printf '%s\n' 'deployment-readiness-agent' ;;
    'production_runbook') printf '%s\n' 'deployment-readiness-agent' ;;
    'deployment_record') printf '%s\n' 'deployment-readiness-agent' ;;
    'project_value_review') printf '%s\n' 'as-built-closeout-agent' ;;
    'project_as_built') printf '%s\n' 'as-built-closeout-agent' ;;
    'supporting_design') printf '%s\n' 'architecture-agent' ;;
    *) return 1 ;;
  esac
}

gendev_artifact_identity_contract_state() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'project_manifest') printf '%s\n' 'current' ;;
    'gate_log') printf '%s\n' 'current' ;;
    'vision') printf '%s\n' 'current' ;;
    'prd') printf '%s\n' 'current' ;;
    'architecture') printf '%s\n' 'current' ;;
    'technology_stack') printf '%s\n' 'current' ;;
    'governance_security') printf '%s\n' 'current' ;;
    'phase_plan') printf '%s\n' 'current' ;;
    'phase_build_plan') printf '%s\n' 'current' ;;
    'tactical_plan') printf '%s\n' 'current' ;;
    'construction_directive') printf '%s\n' 'planned' ;;
    'build_prompt') printf '%s\n' 'planned' ;;
    'phase_test_uat') printf '%s\n' 'current' ;;
    'implementation_evidence') printf '%s\n' 'planned' ;;
    'phase_code_review') printf '%s\n' 'current' ;;
    'phase_remediation') printf '%s\n' 'planned' ;;
    'phase_learnings') printf '%s\n' 'planned' ;;
    'phase_as_built') printf '%s\n' 'current' ;;
    'phase_value_review') printf '%s\n' 'current' ;;
    'traceability') printf '%s\n' 'current' ;;
    'implementation_summary') printf '%s\n' 'planned' ;;
    'final_code_review') printf '%s\n' 'planned' ;;
    'aggregate_remediation') printf '%s\n' 'planned' ;;
    'final_test_uat') printf '%s\n' 'planned' ;;
    'deployment_readiness') printf '%s\n' 'planned' ;;
    'production_runbook') printf '%s\n' 'planned' ;;
    'deployment_record') printf '%s\n' 'planned' ;;
    'project_value_review') printf '%s\n' 'planned' ;;
    'project_as_built') printf '%s\n' 'planned' ;;
    'supporting_design') printf '%s\n' 'planned' ;;
    *) return 1 ;;
  esac
}

gendev_artifact_allowed_statuses() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'project_manifest') printf '%s\n' '' ;;
    'gate_log') printf '%s\n' '' ;;
    'vision') printf '%s\n' 'Draft Ready for Review Ready for Approval Accepted Stale Superseded' ;;
    'prd') printf '%s\n' 'Draft Ready for Review Ready for Approval Accepted Stale Superseded' ;;
    'architecture') printf '%s\n' 'Draft Ready for Review Ready for Approval Accepted Stale Superseded' ;;
    'technology_stack') printf '%s\n' 'Draft Ready for Review Ready for Approval Accepted Stale Superseded' ;;
    'governance_security') printf '%s\n' 'Draft Ready for Review Ready for Approval Accepted Stale Superseded' ;;
    'phase_plan') printf '%s\n' 'Draft Ready for Review Ready for Approval Accepted Stale Superseded' ;;
    'phase_build_plan') printf '%s\n' 'Draft Ready for Review Ready for Approval Accepted Stale Superseded' ;;
    'tactical_plan') printf '%s\n' 'Draft Ready for Review Ready for Approval Accepted Stale Superseded' ;;
    'construction_directive') printf '%s\n' 'Draft Ready for Review Ready for Approval Accepted Stale Superseded' ;;
    'build_prompt') printf '%s\n' 'Draft Ready for Review Ready for Approval Accepted Stale Superseded' ;;
    'phase_test_uat') printf '%s\n' 'Draft Ready for Review Ready for Approval Accepted Complete Stale Superseded' ;;
    'implementation_evidence') printf '%s\n' 'Draft Ready for Review Complete Stale Superseded' ;;
    'phase_code_review') printf '%s\n' 'Draft Ready for Review Complete Stale Superseded' ;;
    'phase_remediation') printf '%s\n' 'Draft Ready for Review Complete Stale Superseded' ;;
    'phase_learnings') printf '%s\n' 'Draft Ready for Review Ready for Approval Accepted Stale Superseded' ;;
    'phase_as_built') printf '%s\n' 'Draft Ready for Review Complete Stale Superseded' ;;
    'phase_value_review') printf '%s\n' 'Draft Ready for Review Complete Stale Superseded' ;;
    'traceability') printf '%s\n' 'Draft Ready for Review Complete Stale Superseded' ;;
    'implementation_summary') printf '%s\n' 'Draft Ready for Review Complete Stale Superseded' ;;
    'final_code_review') printf '%s\n' 'Draft Ready for Review Complete Stale Superseded' ;;
    'aggregate_remediation') printf '%s\n' 'Draft Ready for Review Complete Stale Superseded' ;;
    'final_test_uat') printf '%s\n' 'Draft Ready for Review Complete Stale Superseded' ;;
    'deployment_readiness') printf '%s\n' 'Draft Ready for Review Ready for Approval Accepted Stale Superseded' ;;
    'production_runbook') printf '%s\n' 'Draft Ready for Review Complete Stale Superseded' ;;
    'deployment_record') printf '%s\n' 'Draft Ready for Review Complete Stale Superseded' ;;
    'project_value_review') printf '%s\n' 'Draft Ready for Review Complete Stale Superseded' ;;
    'project_as_built') printf '%s\n' 'Draft Ready for Review Complete Stale Superseded' ;;
    'supporting_design') printf '%s\n' 'Draft Ready for Review Ready for Approval Accepted Stale Superseded' ;;
    *) return 1 ;;
  esac
}

gendev_artifact_lifecycle_bindings() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'project_manifest') printf '%s\n' 'G0 G1 G2 G3 G4 G5 G6 G7 G8 G9' ;;
    'gate_log') printf '%s\n' 'G0 G1 G2 G3 G4 G5 G6 G7 G8 G9' ;;
    'vision') printf '%s\n' 'G1' ;;
    'prd') printf '%s\n' 'G2' ;;
    'architecture') printf '%s\n' 'G3' ;;
    'technology_stack') printf '%s\n' 'G3' ;;
    'governance_security') printf '%s\n' 'G4' ;;
    'phase_plan') printf '%s\n' 'G5.0 G5' ;;
    'phase_build_plan') printf '%s\n' 'G5.<id>.1' ;;
    'tactical_plan') printf '%s\n' 'G5.<id>.2' ;;
    'construction_directive') printf '%s\n' 'G5.<id>.3' ;;
    'build_prompt') printf '%s\n' 'G5.<id>.3' ;;
    'phase_test_uat') printf '%s\n' 'G5.<id>.1 G5.<id>.3 G5.<id>.4' ;;
    'implementation_evidence') printf '%s\n' 'G5.<id>.4' ;;
    'phase_code_review') printf '%s\n' 'G5.<id>.4' ;;
    'phase_remediation') printf '%s\n' 'G5.<id>.4' ;;
    'phase_learnings') printf '%s\n' 'G5.<id>.4' ;;
    'phase_as_built') printf '%s\n' 'G5.<id>.4' ;;
    'phase_value_review') printf '%s\n' 'G5.<id>.4 G9' ;;
    'traceability') printf '%s\n' 'G5.0 G5.<id>.4 G5 G6 G7 G8 G9' ;;
    'implementation_summary') printf '%s\n' 'G6' ;;
    'final_code_review') printf '%s\n' 'G7' ;;
    'aggregate_remediation') printf '%s\n' 'G7' ;;
    'final_test_uat') printf '%s\n' 'G7' ;;
    'deployment_readiness') printf '%s\n' 'G8' ;;
    'production_runbook') printf '%s\n' 'G8' ;;
    'deployment_record') printf '%s\n' 'G9' ;;
    'project_value_review') printf '%s\n' 'G9' ;;
    'project_as_built') printf '%s\n' 'G9' ;;
    'supporting_design') printf '%s\n' 'G3 G4 G5' ;;
    *) return 1 ;;
  esac
}

gendev_artifact_required_work_package() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'project_manifest') printf '%s\n' '' ;;
    'gate_log') printf '%s\n' '' ;;
    'vision') printf '%s\n' '' ;;
    'prd') printf '%s\n' '' ;;
    'architecture') printf '%s\n' '' ;;
    'technology_stack') printf '%s\n' '' ;;
    'governance_security') printf '%s\n' '' ;;
    'phase_plan') printf '%s\n' '' ;;
    'phase_build_plan') printf '%s\n' '' ;;
    'tactical_plan') printf '%s\n' '' ;;
    'construction_directive') printf '%s\n' 'WP-04' ;;
    'build_prompt') printf '%s\n' 'WP-04' ;;
    'phase_test_uat') printf '%s\n' '' ;;
    'implementation_evidence') printf '%s\n' 'WP-04' ;;
    'phase_code_review') printf '%s\n' '' ;;
    'phase_remediation') printf '%s\n' 'WP-04' ;;
    'phase_learnings') printf '%s\n' 'WP-04' ;;
    'phase_as_built') printf '%s\n' '' ;;
    'phase_value_review') printf '%s\n' '' ;;
    'traceability') printf '%s\n' '' ;;
    'implementation_summary') printf '%s\n' 'WP-04' ;;
    'final_code_review') printf '%s\n' 'WP-04' ;;
    'aggregate_remediation') printf '%s\n' 'WP-04' ;;
    'final_test_uat') printf '%s\n' 'WP-04' ;;
    'deployment_readiness') printf '%s\n' 'WP-04' ;;
    'production_runbook') printf '%s\n' 'WP-04' ;;
    'deployment_record') printf '%s\n' 'WP-04' ;;
    'project_value_review') printf '%s\n' 'WP-04' ;;
    'project_as_built') printf '%s\n' 'WP-04' ;;
    'supporting_design') printf '%s\n' 'WP-04' ;;
    *) return 1 ;;
  esac
}

gendev_artifact_project_identity_required() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'project_manifest') printf '%s\n' 'true' ;;
    'gate_log') printf '%s\n' 'true' ;;
    'vision') printf '%s\n' 'true' ;;
    'prd') printf '%s\n' 'true' ;;
    'architecture') printf '%s\n' 'true' ;;
    'technology_stack') printf '%s\n' 'true' ;;
    'governance_security') printf '%s\n' 'true' ;;
    'phase_plan') printf '%s\n' 'true' ;;
    'phase_build_plan') printf '%s\n' 'true' ;;
    'tactical_plan') printf '%s\n' 'true' ;;
    'construction_directive') printf '%s\n' 'true' ;;
    'build_prompt') printf '%s\n' 'true' ;;
    'phase_test_uat') printf '%s\n' 'true' ;;
    'implementation_evidence') printf '%s\n' 'true' ;;
    'phase_code_review') printf '%s\n' 'true' ;;
    'phase_remediation') printf '%s\n' 'true' ;;
    'phase_learnings') printf '%s\n' 'true' ;;
    'phase_as_built') printf '%s\n' 'true' ;;
    'phase_value_review') printf '%s\n' 'true' ;;
    'traceability') printf '%s\n' 'true' ;;
    'implementation_summary') printf '%s\n' 'true' ;;
    'final_code_review') printf '%s\n' 'true' ;;
    'aggregate_remediation') printf '%s\n' 'true' ;;
    'final_test_uat') printf '%s\n' 'true' ;;
    'deployment_readiness') printf '%s\n' 'true' ;;
    'production_runbook') printf '%s\n' 'true' ;;
    'deployment_record') printf '%s\n' 'true' ;;
    'project_value_review') printf '%s\n' 'true' ;;
    'project_as_built') printf '%s\n' 'true' ;;
    'supporting_design') printf '%s\n' 'true' ;;
    *) return 1 ;;
  esac
}

gendev_artifact_provenance_required() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'project_manifest') printf '%s\n' 'false' ;;
    'gate_log') printf '%s\n' 'false' ;;
    'vision') printf '%s\n' 'true' ;;
    'prd') printf '%s\n' 'true' ;;
    'architecture') printf '%s\n' 'true' ;;
    'technology_stack') printf '%s\n' 'true' ;;
    'governance_security') printf '%s\n' 'true' ;;
    'phase_plan') printf '%s\n' 'true' ;;
    'phase_build_plan') printf '%s\n' 'true' ;;
    'tactical_plan') printf '%s\n' 'true' ;;
    'construction_directive') printf '%s\n' 'true' ;;
    'build_prompt') printf '%s\n' 'true' ;;
    'phase_test_uat') printf '%s\n' 'true' ;;
    'implementation_evidence') printf '%s\n' 'true' ;;
    'phase_code_review') printf '%s\n' 'true' ;;
    'phase_remediation') printf '%s\n' 'true' ;;
    'phase_learnings') printf '%s\n' 'true' ;;
    'phase_as_built') printf '%s\n' 'true' ;;
    'phase_value_review') printf '%s\n' 'true' ;;
    'traceability') printf '%s\n' 'true' ;;
    'implementation_summary') printf '%s\n' 'true' ;;
    'final_code_review') printf '%s\n' 'true' ;;
    'aggregate_remediation') printf '%s\n' 'true' ;;
    'final_test_uat') printf '%s\n' 'true' ;;
    'deployment_readiness') printf '%s\n' 'true' ;;
    'production_runbook') printf '%s\n' 'true' ;;
    'deployment_record') printf '%s\n' 'true' ;;
    'project_value_review') printf '%s\n' 'true' ;;
    'project_as_built') printf '%s\n' 'true' ;;
    'supporting_design') printf '%s\n' 'true' ;;
    *) return 1 ;;
  esac
}

gendev_value_review_item_contract() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'complete') printf '%s\n' 'value_result_item' ;;
    'not_due') printf '%s\n' '' ;;
    'not_applicable') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

gendev_event_required_fields() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'project_initialization') printf '%s\n' 'event_id event_type schema_version project from_gate to_gate occurred_on manifest_result checked_statement' ;;
    'gate_transition') printf '%s\n' 'event_id event_type schema_version project from_gate to_gate criterion_ids evidence approval_profile checked_statement enforcement_context manifest_result next_state' ;;
    'phase_checkpoint') printf '%s\n' 'event_id event_type schema_version project major_gate position phase_id criterion_ids evidence references decision approver approved_on checked_statement risk_disposition enforcement_context manifest_result next_state' ;;
    'phase_transition') printf '%s\n' 'event_id event_type schema_version project major_gate position phase_id candidate_revision criterion_ids evidence references test_uat_execution blocking_finding_count remediation_disposition phase_requirement_ids regression_result decision approver approved_on checked_statement risk_disposition coverage_result residual_findings amendments enforcement_context manifest_result next_state' ;;
    'deployment_approval') printf '%s\n' 'event_id event_type schema_version project major_gate release_candidate deployment_intent criterion_ids evidence value_prerequisites decision approver approved_on checked_statement security_approval risk_disposition enforcement_context manifest_result next_state' ;;
    'amendment') printf '%s\n' 'event_id event_type schema_version project authority_path change_scope impact evidence decision approver approved_on checked_statement risk_disposition enforcement_context next_state' ;;
    'gate_regression') printf '%s\n' 'event_id event_type schema_version project from_gate to_gate invalidated_criteria decision approver approved_on checked_statement risk_disposition enforcement_context manifest_result next_state' ;;
    'reconciliation') printf '%s\n' 'event_id event_type schema_version project amendment_id artifacts remaining_stale_artifacts gate_movement_unblocked reconciled_on reconciled_by checked_statement manifest_result' ;;
    'migration_reconciliation') printf '%s\n' 'event_id event_type schema_version project source_methodology_version target_methodology_version historical_event_reference mapped_gate_or_checkpoint mapped_evidence_classes unresolved_fields provenance decision approval_disposition risk_disposition checked_statement' ;;
    'traceability_sample') printf '%s\n' 'event_id event_type schema_version project gate phase_id sampled_by sampled_on requirement_id traceability_row result discrepancy discrepancy_disposition' ;;
    'enforcement_attestation') printf '%s\n' 'event_id event_type schema_version project gate attested_by attested_on requirements_checked result exceptions' ;;
    'enforcement_override') printf '%s\n' 'event_id event_type schema_version project gate decision approved_by approved_on requirements_bypassed reason incident_or_emergency checked_statement risk_disposition enforcement_context normal_enforcement_resumed_on reconciliation_required next_state' ;;
    *) return 1 ;;
  esac
}

gendev_event_conditional_profiles() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'project_initialization') printf '%s\n' '' ;;
    'gate_transition') printf '%s\n' 'named_human no_additional_approval G8-to-G9 G8-to-G9:deploy' ;;
    'phase_checkpoint') printf '%s\n' '' ;;
    'phase_transition') printf '%s\n' 'delegated_phase_exit' ;;
    'deployment_approval') printf '%s\n' 'non_deployment' ;;
    'amendment') printf '%s\n' '' ;;
    'gate_regression') printf '%s\n' '' ;;
    'reconciliation') printf '%s\n' '' ;;
    'migration_reconciliation') printf '%s\n' 'named_human_required duplicate_mapping' ;;
    'traceability_sample') printf '%s\n' '' ;;
    'enforcement_attestation') printf '%s\n' '' ;;
    'enforcement_override') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

gendev_event_conditional_fields() {
  [ "$#" -eq 2 ] || return 2
  case "$1:$2" in
    'gate_transition:named_human') printf '%s\n' 'decision approver approved_on risk_disposition' ;;
    'gate_transition:no_additional_approval') printf '%s\n' 'approval_disposition recorded_by recorded_on no_additional_approval_basis' ;;
    'gate_transition:G8-to-G9') printf '%s\n' 'deployment_disposition operational_results value_disposition terminal_closeout' ;;
    'gate_transition:G8-to-G9:deploy') printf '%s\n' 'operational_owner_confirmation' ;;
    'phase_transition:delegated_phase_exit') printf '%s\n' 'delegation' ;;
    'deployment_approval:non_deployment') printf '%s\n' 'disposition rationale scope release_candidate approver approved_on future_trigger_or_finality' ;;
    'migration_reconciliation:named_human_required') printf '%s\n' 'approver approval_date' ;;
    'migration_reconciliation:duplicate_mapping') printf '%s\n' 'supersedes_event_id correction_reason' ;;
    *) return 1 ;;
  esac
}

gendev_event_schema_version() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'project_initialization') printf '%s\n' '2' ;;
    'gate_transition') printf '%s\n' '2' ;;
    'phase_checkpoint') printf '%s\n' '2' ;;
    'phase_transition') printf '%s\n' '2' ;;
    'deployment_approval') printf '%s\n' '2' ;;
    'amendment') printf '%s\n' '2' ;;
    'gate_regression') printf '%s\n' '2' ;;
    'reconciliation') printf '%s\n' '2' ;;
    'migration_reconciliation') printf '%s\n' '2' ;;
    'traceability_sample') printf '%s\n' '2' ;;
    'enforcement_attestation') printf '%s\n' '2' ;;
    'enforcement_override') printf '%s\n' '2' ;;
    *) return 1 ;;
  esac
}

gendev_event_append_only() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'project_initialization') printf '%s\n' 'true' ;;
    'gate_transition') printf '%s\n' 'true' ;;
    'phase_checkpoint') printf '%s\n' 'true' ;;
    'phase_transition') printf '%s\n' 'true' ;;
    'deployment_approval') printf '%s\n' 'true' ;;
    'amendment') printf '%s\n' 'true' ;;
    'gate_regression') printf '%s\n' 'true' ;;
    'reconciliation') printf '%s\n' 'true' ;;
    'migration_reconciliation') printf '%s\n' 'true' ;;
    'traceability_sample') printf '%s\n' 'true' ;;
    'enforcement_attestation') printf '%s\n' 'true' ;;
    'enforcement_override') printf '%s\n' 'true' ;;
    *) return 1 ;;
  esac
}

gendev_event_changes_major_gate() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'project_initialization') printf '%s\n' 'true' ;;
    'gate_transition') printf '%s\n' 'true' ;;
    'phase_checkpoint') printf '%s\n' 'false' ;;
    'phase_transition') printf '%s\n' 'false' ;;
    'deployment_approval') printf '%s\n' 'false' ;;
    'amendment') printf '%s\n' 'false' ;;
    'gate_regression') printf '%s\n' 'true' ;;
    'reconciliation') printf '%s\n' 'false' ;;
    'migration_reconciliation') printf '%s\n' 'false' ;;
    'traceability_sample') printf '%s\n' 'false' ;;
    'enforcement_attestation') printf '%s\n' 'false' ;;
    'enforcement_override') printf '%s\n' 'false' ;;
    *) return 1 ;;
  esac
}

gendev_evidence_reviewed_status() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'new_acceptance_status_only') printf '%s\n' 'Ready for Approval' ;;
    'complete_report_unchanged') printf '%s\n' 'Complete' ;;
    'accepted_authority_unchanged') printf '%s\n' 'Accepted' ;;
    *) return 1 ;;
  esac
}

gendev_evidence_resulting_status() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'new_acceptance_status_only') printf '%s\n' 'Accepted' ;;
    'complete_report_unchanged') printf '%s\n' 'Complete' ;;
    'accepted_authority_unchanged') printf '%s\n' 'Accepted' ;;
    *) return 1 ;;
  esac
}

gendev_artifact_status_is_valid() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'Draft') printf '%s\n' 'true' ;;
    'Ready for Review') printf '%s\n' 'true' ;;
    'Ready for Approval') printf '%s\n' 'true' ;;
    'Accepted') printf '%s\n' 'true' ;;
    'Complete') printf '%s\n' 'true' ;;
    'Stale') printf '%s\n' 'true' ;;
    'Superseded') printf '%s\n' 'true' ;;
    *) return 1 ;;
  esac
}

gendev_deployment_artifact_reviewed_statuses() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'deployment_readiness') printf '%s\n' 'Ready for Approval' ;;
    'production_runbook') printf '%s\n' 'Complete' ;;
    *) return 1 ;;
  esac
}

gendev_deployment_artifact_resulting_statuses() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'deployment_readiness') printf '%s\n' 'Accepted' ;;
    'production_runbook') printf '%s\n' 'Complete' ;;
    *) return 1 ;;
  esac
}

gendev_deployment_artifact_evidence_categories() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'deployment_readiness') printf '%s\n' 'new_acceptance_status_only' ;;
    'production_runbook') printf '%s\n' 'complete_report_unchanged' ;;
    *) return 1 ;;
  esac
}

gendev_deployment_artifact_required_dispositions() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'deployment_readiness') printf '%s\n' '' ;;
    'production_runbook') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

gendev_value_review_required_fields() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'complete') printf '%s\n' 'criterion_results' ;;
    'not_due') printf '%s\n' 'trigger expected_date_when_knowable owner evidence_source next_review_mechanism' ;;
    'not_applicable') printf '%s\n' 'rationale accepted_by accepted_on' ;;
    *) return 1 ;;
  esac
}

gendev_value_review_allowed_results() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'complete') printf '%s\n' 'met missed unmeasurable' ;;
    'not_due') printf '%s\n' '' ;;
    'not_applicable') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

gendev_value_review_follow_up_required_for() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'complete') printf '%s\n' 'missed unmeasurable' ;;
    'not_due') printf '%s\n' '' ;;
    'not_applicable') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

gendev_deployment_terminal_disposition() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'deploy') printf '%s\n' 'deployed' ;;
    'non_deployment') printf '%s\n' 'not_deployed' ;;
    *) return 1 ;;
  esac
}

gendev_deployment_production_action_performed() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'deploy') printf '%s\n' 'true' ;;
    'non_deployment') printf '%s\n' 'false' ;;
    *) return 1 ;;
  esac
}

gendev_event_binding_event_types() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'all_declared_phase_exit_event_ids') printf '%s\n' 'phase_transition' ;;
    'accepted_upstream_authority_set') printf '%s\n' 'gate_transition phase_checkpoint' ;;
    'deployment_approval_or_non_deployment_approval') printf '%s\n' 'deployment_approval' ;;
    *) return 1 ;;
  esac
}

gendev_event_binding_allowed_intents() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'all_declared_phase_exit_event_ids') printf '%s\n' '' ;;
    'accepted_upstream_authority_set') printf '%s\n' '' ;;
    'deployment_approval_or_non_deployment_approval') printf '%s\n' 'deploy non_deployment' ;;
    *) return 1 ;;
  esac
}

gendev_event_binding_allowed_decisions() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'all_declared_phase_exit_event_ids') printf '%s\n' 'approved' ;;
    'accepted_upstream_authority_set') printf '%s\n' '' ;;
    'deployment_approval_or_non_deployment_approval') printf '%s\n' 'approved' ;;
    *) return 1 ;;
  esac
}

gendev_event_binding_quantifier() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'all_declared_phase_exit_event_ids') printf '%s\n' 'exactly_one_per_declared_nonsuperseded_phase' ;;
    'accepted_upstream_authority_set') printf '%s\n' 'every_reused_accepted_authority' ;;
    'deployment_approval_or_non_deployment_approval') printf '%s\n' 'exactly_one_latest_unsuperseded' ;;
    *) return 1 ;;
  esac
}

gendev_event_binding_position_pattern() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'all_declared_phase_exit_event_ids') printf '%s\n' '^G5\.[A-Za-z0-9]+(-[A-Za-z0-9]+)*\.4$' ;;
    'accepted_upstream_authority_set') printf '%s\n' '' ;;
    'deployment_approval_or_non_deployment_approval') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

gendev_event_binding_evidence_category() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'all_declared_phase_exit_event_ids') printf '%s\n' '' ;;
    'accepted_upstream_authority_set') printf '%s\n' 'accepted_authority_unchanged' ;;
    'deployment_approval_or_non_deployment_approval') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

gendev_event_binding_coverage_source() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'all_declared_phase_exit_event_ids') printf '%s\n' 'manifest.phase.phases' ;;
    'accepted_upstream_authority_set') printf '%s\n' '' ;;
    'deployment_approval_or_non_deployment_approval') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

gendev_event_binding_major_gate() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'all_declared_phase_exit_event_ids') printf '%s\n' '' ;;
    'accepted_upstream_authority_set') printf '%s\n' '' ;;
    'deployment_approval_or_non_deployment_approval') printf '%s\n' 'G8' ;;
    *) return 1 ;;
  esac
}

gendev_event_binding_criterion_source() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'all_declared_phase_exit_event_ids') printf '%s\n' '' ;;
    'accepted_upstream_authority_set') printf '%s\n' '' ;;
    'deployment_approval_or_non_deployment_approval') printf '%s\n' 'deployment.criterion_ids' ;;
    *) return 1 ;;
  esac
}

gendev_event_binding_terminal_correlation() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'all_declared_phase_exit_event_ids') printf '%s\n' '' ;;
    'accepted_upstream_authority_set') printf '%s\n' '' ;;
    'deployment_approval_or_non_deployment_approval') printf '%s\n' 'deployment.terminal_correlations' ;;
    *) return 1 ;;
  esac
}

gendev_event_binding_required_flags() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'all_declared_phase_exit_event_ids') printf '%s\n' 'same_project_required exact_candidate_required' ;;
    'accepted_upstream_authority_set') printf '%s\n' 'originating_event_required same_path_and_blob_required' ;;
    'deployment_approval_or_non_deployment_approval') printf '%s\n' 'same_project_required exact_release_candidate_required' ;;
    *) return 1 ;;
  esac
}

gendev_reference_rule() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'REF-TARGET-KIND') printf '%s\n' 'Every typed reference declares exactly one target kind.' ;;
    'REF-CANONICAL-PATH') printf '%s\n' 'Canonical authority and evidence references resolve to registered canonical paths.' ;;
    'REF-SUPPORTING-PATH') printf '%s\n' 'Supporting-design references resolve only below docs/project/design/.' ;;
    'REF-AUTHORITY-DIRECTION') printf '%s\n' 'Supporting artifacts cannot govern canonical authority.' ;;
    'REF-NO-CYCLE') printf '%s\n' 'All typed reference edges participate in one directed acyclic graph; approved depth exceptions do not permit cycles.' ;;
    'REF-DEPTH') printf '%s\n' 'Supporting-design references are one level deep by default; greater depth requires a declared, justified, named-human-approved exception.' ;;
    *) return 1 ;;
  esac
}

gendev_reference_target_scope() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'canonical_artifact') printf '%s\n' 'canonical_fixed_or_phase_artifact' ;;
    'supporting_design') printf '%s\n' 'docs/project/design/' ;;
    *) return 1 ;;
  esac
}

gendev_reference_authority_direction() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'canonical_artifact') printf '%s\n' 'derive_from_registered_artifact_kind' ;;
    'supporting_design') printf '%s\n' 'support_only' ;;
    *) return 1 ;;
  esac
}

gendev_reference_identity_contract() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'canonical_artifact') printf '%s\n' 'registered_artifact_identity_and_provenance' ;;
    'supporting_design') printf '%s\n' 'project_identity_and_derived_from_canonical_source' ;;
    *) return 1 ;;
  esac
}

gendev_reference_form_contract() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'canonical_artifact') printf '%s\n' '' ;;
    'supporting_design') printf '%s\n' 'textual_technique_specific_design' ;;
    *) return 1 ;;
  esac
}

gendev_reference_lifecycle_owner() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'canonical_artifact') printf '%s\n' 'registered_artifact_owner_and_bindings' ;;
    'supporting_design') printf '%s\n' 'declared_canonical_source_owner' ;;
    *) return 1 ;;
  esac
}

gendev_reference_cycle_policy() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'canonical_artifact') printf '%s\n' 'global_reference_dag' ;;
    'supporting_design') printf '%s\n' 'global_reference_dag' ;;
    *) return 1 ;;
  esac
}

gendev_reference_depth_policy() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'canonical_artifact') printf '%s\n' 'registered_canonical_lifecycle' ;;
    'supporting_design') printf '%s\n' 'default_one_or_approved_exception' ;;
    *) return 1 ;;
  esac
}

gendev_reference_validation_severity() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'canonical_artifact') printf '%s\n' 'error_when_mechanical_warning_when_judgment' ;;
    'supporting_design') printf '%s\n' 'error_when_mechanical_warning_when_judgment' ;;
    *) return 1 ;;
  esac
}

gendev_scaling_requirements_form() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'C1') printf '%s\n' 'plain_observable_or_ears' ;;
    'C2') printf '%s\n' 'ears' ;;
    'C3') printf '%s\n' 'ears' ;;
    *) return 1 ;;
  esac
}

gendev_scaling_gate_combination() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'C1') printf '%s\n' 'G1-G4 may combine with explicit content preservation, evidence, named-human approval, and recorded justification.' ;;
    'C2') printf '%s\n' 'Full chain by default; a combination requires explicit span, preserved criteria, justification, and named-human approval.' ;;
    'C3') printf '%s\n' 'prohibited' ;;
    *) return 1 ;;
  esac
}

gendev_scaling_label() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'C1') printf '%s\n' 'Contained' ;;
    'C2') printf '%s\n' 'Standard' ;;
    'C3') printf '%s\n' 'Critical' ;;
    *) return 1 ;;
  esac
}

gendev_scaling_design_interrogation() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'C1') printf '%s\n' 'proportional' ;;
    'C2') printf '%s\n' 'proportional' ;;
    'C3') printf '%s\n' 'expanded' ;;
    *) return 1 ;;
  esac
}

gendev_scaling_unwanted_behavior_required() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'C1') printf '%s\n' 'true' ;;
    'C2') printf '%s\n' 'true' ;;
    'C3') printf '%s\n' 'true' ;;
    *) return 1 ;;
  esac
}

gendev_scaling_verification_spec_required() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'C1') printf '%s\n' 'true' ;;
    'C2') printf '%s\n' 'true' ;;
    'C3') printf '%s\n' 'true' ;;
    *) return 1 ;;
  esac
}

gendev_scaling_phase_exit_evidence_waivable() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'C1') printf '%s\n' 'false' ;;
    'C2') printf '%s\n' 'false' ;;
    'C3') printf '%s\n' 'false' ;;
    *) return 1 ;;
  esac
}

gendev_scaling_g2_required_all() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'C1') printf '%s\n' '' ;;
    'C2') printf '%s\n' 'G2-EARS-FORM' ;;
    'C3') printf '%s\n' 'G2-EARS-FORM' ;;
    *) return 1 ;;
  esac
}

gendev_scaling_g2_required_any() {
  [ "$#" -eq 1 ] || return 2
  case "$1" in
    'C1') printf '%s\n' 'G2-EARS-FORM G2-OBSERVABLE-FORM' ;;
    'C2') printf '%s\n' '' ;;
    'C3') printf '%s\n' '' ;;
    *) return 1 ;;
  esac
}

readonly GENDEV_LIFECYCLE_CONTRACT_LOADED='252487aed05f12f2c4c296938f8a6d797ac2a0c32ddee16e7c54a8e797826370' || {
  printf '%s\n' 'lifecycle contract: conflicting GENDEV_LIFECYCLE_CONTRACT_LOADED' >&2
  return 2 2>/dev/null || exit 2
}
